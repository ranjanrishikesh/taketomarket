---
phase: 02-onboarding-interview
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - skills/ttm-init/SKILL.md
autonomous: false
requirements:
  - ONBD-01
  - ONBD-05
  - ONBD-09

must_haves:
  truths:
    - "SKILL.md routes to the workflow file without stub placeholder text"
    - "User can visually confirm /ttm-init workflow is functional"
  artifacts:
    - path: "skills/ttm-init/SKILL.md"
      provides: "Active skill routing to init workflow"
      contains: "workflows/setup/init.md"
  key_links:
    - from: "skills/ttm-init/SKILL.md"
      to: "workflows/setup/init.md"
      via: "Workflow reference in skill body"
      pattern: "workflows/setup/init.md"
---

<objective>
Update the /ttm-init SKILL.md stub to remove the "Not yet implemented" status and verify the complete onboarding workflow is functional via human checkpoint.

Purpose: The SKILL.md stub was created in Phase 1 as a placeholder. Now that the workflow exists, the stub needs to route cleanly to it. The human checkpoint confirms the interview flow works end-to-end.
Output: Updated SKILL.md + human verification of the /ttm-init workflow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@skills/ttm-init/SKILL.md
@workflows/setup/init.md

<interfaces>
From skills/ttm-init/SKILL.md (current content to modify):
```yaml
---
name: ttm-init
description: >
  Interview-driven onboarding that generates all .marketing/ reference files
  from structured questioning. Use when setting up takeToMarket for a new project.
disable-model-invocation: true
allowed-tools: Read Write Bash Glob Grep
---

# /ttm-init

**Status:** Not yet implemented (Phase 2)

Read and follow the workflow at `${CLAUDE_PLUGIN_ROOT}/workflows/setup/init.md`
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update SKILL.md to remove stub status and finalize routing</name>
  <files>skills/ttm-init/SKILL.md</files>
  <read_first>
    - skills/ttm-init/SKILL.md (current content)
    - workflows/setup/init.md (verify workflow exists)
    - .planning/phases/02-onboarding-interview/02-PATTERNS.md (SKILL.md target state section)
  </read_first>
  <action>
Update `skills/ttm-init/SKILL.md` to its final state. The file should contain:

```markdown
---
name: ttm-init
description: >
  Interview-driven onboarding that generates all .marketing/ reference files
  from structured questioning. Use when setting up takeToMarket for a new project.
disable-model-invocation: true
allowed-tools: Read Write Bash Glob Grep
---

# /ttm-init

Read and follow the workflow at `${CLAUDE_PLUGIN_ROOT}/workflows/setup/init.md`
```

Changes from current state:
1. REMOVE the line `**Status:** Not yet implemented (Phase 2)`
2. REMOVE the bullet list explaining what the command does (that information is now in the workflow file itself)
3. KEEP the YAML frontmatter exactly as-is (name, description, disable-model-invocation, allowed-tools)
4. KEEP the H1 heading `# /ttm-init`
5. KEEP the workflow reference line

The resulting file should be exactly 12 lines (3 frontmatter delimiter lines + 5 frontmatter content lines + 1 blank + 1 heading + 1 blank + 1 workflow reference).
  </action>
  <verify>
    <automated>grep -q "Not yet implemented" skills/ttm-init/SKILL.md && echo "FAIL: still has stub text" || (grep -q "workflows/setup/init.md" skills/ttm-init/SKILL.md && grep -q "name: ttm-init" skills/ttm-init/SKILL.md && echo "PASS")</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Not yet implemented" skills/ttm-init/SKILL.md` FAILS (stub text removed)
    - `grep -q "This command will" skills/ttm-init/SKILL.md` FAILS (bullet list removed)
    - `grep -q "name: ttm-init" skills/ttm-init/SKILL.md` succeeds (frontmatter preserved)
    - `grep -q "disable-model-invocation: true" skills/ttm-init/SKILL.md` succeeds
    - `grep -q "allowed-tools: Read Write Bash Glob Grep" skills/ttm-init/SKILL.md` succeeds
    - `grep -q "workflows/setup/init.md" skills/ttm-init/SKILL.md` succeeds (workflow routing)
    - `wc -l < skills/ttm-init/SKILL.md` returns 12 or fewer lines
  </acceptance_criteria>
  <done>SKILL.md contains only frontmatter and workflow routing -- no stub text, no bullet list, under 15 lines</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify /ttm-init end-to-end workflow</name>
  <files>workflows/setup/init.md</files>
  <action>
Human verifies the complete /ttm-init onboarding interview workflow works end-to-end. This checkpoint confirms:
- Pre-flight check for existing .marketing/ directory
- 6-section structured interview (Product/Positioning, Brand/Voice, ICP/Audience, Channels, Competitors, Metrics/Calendar)
- Specificity validation that rejects vague answers with re-prompts
- Generation of all 9 .marketing/ reference files from templates
- Generation of CLAUDE.md and AGENTS.md instruction files
- Post-init health check and state update

Steps:
1. In a Claude Code session with the takeToMarket plugin installed, run `/ttm-init`
2. Verify the pre-flight check detects no existing .marketing/ and creates directories
3. Answer the interview questions across all 6 sections:
   - Section 1: Give a specific product description and differentiator (test: try giving a vague answer like "innovative solution" first -- it should be rejected with a specificity re-prompt)
   - Section 2: Choose a voice archetype and provide brand examples
   - Section 3: Describe your ideal customer with specific demographics
   - Section 4: List active marketing channels
   - Section 5: Name 2-3 competitors
   - Section 6: Define your primary outcome metric and calendar
4. At the confirmation gate, verify the collected data summary looks correct, then proceed
5. After file generation, check that all 9 files exist in .marketing/:
   - `ls .marketing/*.md` should show 9 files
   - `cat .marketing/POSITIONING.md` should have _SUMMARY/END_SUMMARY markers and your interview data (not placeholder text)
   - `cat .marketing/BRAND.md` should have your voice archetype data
   - `cat .marketing/STATE.md` should have `status: initialized` in frontmatter
   - `cat .marketing/LEARNINGS.md` should have "Total lessons: 0"
6. Verify CLAUDE.md and AGENTS.md exist in project root
7. Verify `node bin/ttm-tools.cjs health --raw` reports HEALTHY
8. Verify `node bin/ttm-tools.cjs init --raw` reports "initialized"
  </action>
  <verify>
    <automated>node bin/ttm-tools.cjs health --raw && node bin/ttm-tools.cjs init --raw</automated>
  </verify>
  <done>Human confirms /ttm-init works end-to-end: interview completes, all 11 files generated, health check passes, state is initialized</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | SKILL.md update is a 2-line change to a config file. Checkpoint is human verification only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Spoofing | SKILL.md routing | accept | SKILL.md is a local file the user controls. No external input surface. |
</threat_model>

<verification>
- SKILL.md routes to workflow without stub placeholder
- Human confirms end-to-end /ttm-init flow works
- All 9 reference files generated with correct structure
- Health check passes after initialization
</verification>

<success_criteria>
- SKILL.md is finalized (no stub text)
- Human has verified /ttm-init works with real interview data
- All 11 output files (9 reference + CLAUDE.md + AGENTS.md) are generated correctly
</success_criteria>

<output>
After completion, create `.planning/phases/02-onboarding-interview/02-03-SUMMARY.md`
</output>
