---
phase: 06-positioning-invariant-system
plan: 05
type: execute
wave: 1
depends_on: ["06-02"]
files_modified:
  - references/positioning-check-report.md
  - workflows/reference-mgmt/positioning-check.md
autonomous: true
gap_closure: true
requirements: [POSN-04]

must_haves:
  truths:
    - "The positioning-check report contains a dedicated Bleeding Analysis section that classifies must-not-say violations by asset type (customer-facing vs non-customer-facing)"
    - "Customer-facing must-not-say violations are labeled as 'bleeding' with higher severity framing than non-customer-facing usage"
    - "The bleeding analysis section appears in every audit report, even when no bleeding is detected (shows clean result)"
  artifacts:
    - path: "references/positioning-check-report.md"
      provides: "Bleeding Analysis report section template"
      contains: "## Bleeding Analysis"
    - path: "workflows/reference-mgmt/positioning-check.md"
      provides: "Bleeding analysis evaluation step"
      contains: "Bleeding Analysis"
  key_links:
    - from: "workflows/reference-mgmt/positioning-check.md"
      to: "references/positioning-check-report.md"
      via: "@-syntax required_reading reference"
      pattern: "Bleeding Analysis"
---

<objective>
Add a dedicated Bleeding Analysis section to the positioning-check workflow and report template. Currently, Check 3 (Must-Not-Say Compliance) distinguishes customer-facing FAIL from non-customer-facing WARN, but results are not framed as "bleeding analysis." The roadmap SC4 requires explicit "bleeding-into-customer-facing-materials analysis" and the SKILL.md description claims "bleeding analysis" that does not exist in the workflow.

Purpose: Close the SC4 verification gap so that the audit report explicitly identifies where positioning violations have bled into customer-facing materials as a distinct concern.
Output: Modified positioning-check.md with a bleeding analysis step, modified positioning-check-report.md with a Bleeding Analysis report section.
</objective>

<execution_context>
@/Users/rishikeshranjan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rishikeshranjan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-positioning-invariant-system/06-02-SUMMARY.md

@references/positioning-check-report.md
@workflows/reference-mgmt/positioning-check.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Bleeding Analysis section to positioning-check-report.md</name>
  <files>references/positioning-check-report.md</files>
  <read_first>
    - references/positioning-check-report.md (full file, 157 lines)
  </read_first>
  <action>
Add a new "## Bleeding Analysis" section to references/positioning-check-report.md. Insert it AFTER the "## Drift Categories" section and BEFORE the "## Per-Asset Drift Calculation" section. This section defines the bleeding analysis concept and classification rules.

Add the following content:

```markdown
## Bleeding Analysis

Bleeding occurs when must-not-say terms from POSITIONING.md appear in **customer-facing** materials. This is distinct from general must-not-say drift -- non-customer-facing usage (internal briefs, planning docs) is a WARN, but customer-facing usage represents positioning "bleeding" into territory the brand explicitly avoids.

### Asset Type Classification

Classify each asset as customer-facing or non-customer-facing based on its file path and content type:

| Asset Type | Classification | Examples |
|------------|---------------|----------|
| Landing pages, blog posts, ad copy, social posts, email campaigns | Customer-facing | Assets intended for external audience consumption |
| Internal briefs, research docs, planning notes, strategy docs | Non-customer-facing | Assets used internally for planning and alignment |

When asset type is ambiguous, classify as customer-facing (conservative default).

### Bleeding Severity

| Check 3 Result | Asset Type | Bleeding Status | Severity |
|----------------|-----------|-----------------|----------|
| FAIL | Customer-facing | BLEEDING | Critical -- must-not-say term reaching external audience |
| WARN | Non-customer-facing | NOT BLEEDING | Advisory -- internal usage, lower risk |
| PASS | Any | CLEAN | No must-not-say terms found |
```

Then, inside the "## Report Template" section (the fenced code block starting with the `========================================` banner), insert a new `## Bleeding Analysis` subsection. Place it AFTER the `## Drift Type Breakdown` table and BEFORE the `## Findings Detail` section. Add this content inside the template code block:

```
## Bleeding Analysis

Must-not-say terms that have bled into customer-facing materials:

| # | Asset | Campaign | Term Found | Context | Severity |
|---|-------|----------|------------|---------|----------|
| 1 | ${ASSET_NAME} | ${CAMPAIGN_SLUG} | "${TERM}" | "${SURROUNDING_CONTEXT}" | Critical |
| ... | ... | ... | ... | ... | ... |

**Bleeding count:** ${BLEEDING_COUNT} of ${MNS_VIOLATION_COUNT} must-not-say violations are in customer-facing materials
**Bleeding rate:** ${BLEEDING_RATE}% of must-not-say violations have bled into customer-facing territory

${IF_NO_BLEEDING}No must-not-say terms found in customer-facing materials. Positioning boundaries are holding.${END_IF}
${IF_BLEEDING}Customer-facing assets contain must-not-say terms. These should be prioritized for /ttm-fix as they represent active positioning leakage to your audience.${END_IF}
```

Do NOT modify any existing sections -- only add the new content.
  </action>
  <verify>
    <automated>grep -c "## Bleeding Analysis" references/positioning-check-report.md | grep -q "2" && grep -c "BLEEDING" references/positioning-check-report.md | grep -qv "^0$" && grep -c "customer-facing" references/positioning-check-report.md | grep -qv "^0$" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "## Bleeding Analysis" references/positioning-check-report.md returns 2 (one in definitions, one in report template)
    - grep -c "Bleeding Severity" references/positioning-check-report.md returns 1
    - grep -c "Bleeding count" references/positioning-check-report.md returns 1
    - grep -c "customer-facing" references/positioning-check-report.md returns at least 5
    - grep -c "NOT BLEEDING" references/positioning-check-report.md returns 1
    - File does not exceed 500 lines: wc -l references/positioning-check-report.md shows less than 500
  </acceptance_criteria>
  <done>positioning-check-report.md contains a Bleeding Analysis definition section with asset type classification and severity table, plus a Bleeding Analysis subsection in the report template with per-violation table, bleeding count, bleeding rate, and conditional messaging for clean vs bleeding results</done>
</task>

<task type="auto">
  <name>Task 2: Add Bleeding Analysis step to positioning-check.md workflow</name>
  <files>workflows/reference-mgmt/positioning-check.md</files>
  <read_first>
    - workflows/reference-mgmt/positioning-check.md (full file, 298 lines)
    - references/positioning-check-report.md (to verify Task 1 output)
  </read_first>
  <action>
Add a new step "## Step 5b: Bleeding Analysis" to workflows/reference-mgmt/positioning-check.md. Insert it AFTER the existing "## Step 5: Calculate Aggregates" section (which ends before "## Step 6: Trend Comparison") and BEFORE "## Step 6: Trend Comparison". Renumber nothing -- use "5b" to avoid disrupting existing step references.

Add the following content:

```markdown
## Step 5b: Bleeding Analysis

```
takeToMarket > ANALYZING MUST-NOT-SAY BLEEDING
```

From the per-asset evaluation results collected in Step 4, extract all assets where
Check 3 (Must-Not-Say Compliance) returned WARN or FAIL.

For each must-not-say violation:

1. **Classify the asset as customer-facing or non-customer-facing:**
   - Customer-facing: landing pages, blog posts, ad copy, social posts, email campaigns,
     any asset intended for external audience consumption
   - Non-customer-facing: internal briefs, research docs, planning notes, strategy docs
   - When ambiguous, classify as customer-facing (conservative default per
     @positioning-check-report.md Bleeding Analysis rules)

2. **Determine bleeding status:**
   - Check 3 = FAIL (customer-facing asset): Mark as **BLEEDING** (Critical)
   - Check 3 = WARN (non-customer-facing asset): Mark as **NOT BLEEDING** (Advisory)

3. **Extract the specific must-not-say term and surrounding context** (10 words before
   and after the term occurrence) for inclusion in the Bleeding Analysis report section.

Calculate bleeding metrics:
- **BLEEDING_COUNT:** Number of assets with Check 3 = FAIL (customer-facing violations)
- **MNS_VIOLATION_COUNT:** Total assets with Check 3 = WARN or FAIL
- **BLEEDING_RATE:** BLEEDING_COUNT / MNS_VIOLATION_COUNT * 100 (0% if no violations)

Display:
```
  Bleeding: ${BLEEDING_COUNT} of ${MNS_VIOLATION_COUNT} must-not-say violations in customer-facing materials
```

These values feed into the Bleeding Analysis section of the report generated in Step 7.
```

Then update the existing "## Step 7: Generate Report" section. Find the line that says:
"Generate the full audit report including:"
Add a new bullet to the list, AFTER "- Drift type breakdown (count per category)" and BEFORE "- Findings detail for every WARN and FAIL result":

```
- Bleeding analysis (must-not-say violations classified by asset type with bleeding count and rate)
```

Then update the `<success_criteria>` section at the bottom of the file. Add a new checklist item AFTER the line about drift categorized by type:

```
- [ ] Bleeding analysis classifies must-not-say violations by asset type (customer-facing vs non-customer-facing)
```
  </action>
  <verify>
    <automated>grep -c "Step 5b" workflows/reference-mgmt/positioning-check.md | grep -q "1" && grep -c "BLEEDING" workflows/reference-mgmt/positioning-check.md | grep -qv "^0$" && grep -c "Bleeding Analysis" workflows/reference-mgmt/positioning-check.md | grep -qv "^0$" && grep -c "bleeding analysis" workflows/reference-mgmt/positioning-check.md | grep -qv "^0$" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "Step 5b" workflows/reference-mgmt/positioning-check.md returns 1
    - grep -c "Bleeding Analysis" workflows/reference-mgmt/positioning-check.md returns at least 1
    - grep -c "BLEEDING_COUNT" workflows/reference-mgmt/positioning-check.md returns at least 2
    - grep -c "BLEEDING_RATE" workflows/reference-mgmt/positioning-check.md returns at least 1
    - grep -c "customer-facing" workflows/reference-mgmt/positioning-check.md returns at least 3
    - grep "success_criteria" -A 20 workflows/reference-mgmt/positioning-check.md | grep -c "Bleeding analysis" returns 1
    - grep "Generate the full audit report" -A 15 workflows/reference-mgmt/positioning-check.md | grep -c "Bleeding analysis" returns 1
    - File does not exceed 500 lines: wc -l workflows/reference-mgmt/positioning-check.md shows less than 500
  </acceptance_criteria>
  <done>positioning-check.md contains Step 5b: Bleeding Analysis that classifies must-not-say violations by asset type, calculates bleeding count and rate, and feeds into the report. The Step 7 report generation bullet list includes bleeding analysis. The success criteria checklist includes bleeding analysis verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan modifies workflow Markdown files only -- no code, no external input, no trust boundaries crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-01 | I (Information Disclosure) | Bleeding analysis context extraction | accept | The 10-word context window around must-not-say terms is displayed in the audit report, which is stdout-only and not persisted to a file. The user already has access to all asset content. No elevated risk. |
</threat_model>

<verification>
After both tasks complete:

1. `grep -c "## Bleeding Analysis" references/positioning-check-report.md` returns 2
2. `grep -c "Step 5b" workflows/reference-mgmt/positioning-check.md` returns 1
3. `grep -c "Bleeding Analysis" workflows/reference-mgmt/positioning-check.md` returns at least 1
4. `wc -l references/positioning-check-report.md` shows less than 500
5. `wc -l workflows/reference-mgmt/positioning-check.md` shows less than 500
6. Both files maintain valid Markdown structure
7. No existing content was removed or modified beyond the specified additions
</verification>

<success_criteria>
- The positioning-check report template has a dedicated "Bleeding Analysis" section that classifies must-not-say violations as BLEEDING (customer-facing) or NOT BLEEDING (non-customer-facing)
- The positioning-check workflow has a Step 5b that performs bleeding analysis between aggregate calculation and trend comparison
- The report template shows per-violation bleeding details with term, context, and severity
- The report template shows bleeding count and bleeding rate metrics
- The report template handles the clean case (no bleeding detected) with a positive message
- The workflow success criteria checklist includes bleeding analysis
- Neither file exceeds 500 lines
- POSN-04 SC4 "bleeding-into-customer-facing-materials analysis" requirement is explicitly satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/06-positioning-invariant-system/06-05-SUMMARY.md`
</output>
