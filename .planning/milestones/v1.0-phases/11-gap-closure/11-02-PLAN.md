---
phase: 11-gap-closure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - install.js
  - bin/lib/health.cjs
autonomous: true
gap_closure: true
requirements:
  - DIST-01
  - DIST-02
  - DIST-03

must_haves:
  truths:
    - "npm/git-clone install copies agents/ directory to target"
    - "Post-install validation checks for agents/ directory presence"
    - "health.cjs validGateValues includes fix_needed and accepted so deviation results do not cause false failures"
  artifacts:
    - path: "install.js"
      provides: "agents/ in DIRS_TO_COPY and validateInstall"
      contains: "'agents'"
    - path: "bin/lib/health.cjs"
      provides: "Complete gate value allowlist"
      contains: "fix_needed"
  key_links:
    - from: "install.js"
      to: "agents/ttm-producer.md"
      via: "DIRS_TO_COPY includes agents"
      pattern: "'agents'"
---

<objective>
Fix the install.js missing agents/ directory (B-02) and the health.cjs incomplete gate value allowlist (W-01) so that npm/git-clone installs include all required directories and health checks do not produce false failures after deviations.

Purpose: B-02 is a blocker -- every npm/git-clone install is missing agents/ttm-producer.md, which breaks /ttm-produce, /ttm-fix, and /ttm-repurpose. W-01 causes false gate consistency failures when campaigns have accepted deviations or assets needing fixes.

Output: Fixed install.js and health.cjs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add agents/ to DIRS_TO_COPY in install.js</name>
  <files>install.js</files>
  <read_first>
    - install.js (current state -- need to see DIRS_TO_COPY array at line 13 and validateInstall at line 124)
  </read_first>
  <action>
In install.js, add `'agents'` to the DIRS_TO_COPY array at line 13.

Current array (lines 13-22):
```javascript
const DIRS_TO_COPY = [
  '.claude-plugin',
  'skills',
  'workflows',
  'templates',
  'references',
  'playbooks',
  'gates',
  'bin',
];
```

Change to:
```javascript
const DIRS_TO_COPY = [
  '.claude-plugin',
  'skills',
  'workflows',
  'templates',
  'references',
  'playbooks',
  'gates',
  'bin',
  'agents',
];
```

The validateInstall function (line 124-164) already iterates over DIRS_TO_COPY to check directory presence, so adding 'agents' to the array automatically adds it to both the copy loop AND the validation loop. No other changes needed in install.js.
  </action>
  <verify>
    <automated>grep -q "'agents'" install.js && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "'agents'" install.js` returns at least 1 match inside the DIRS_TO_COPY array
    - `grep -c "DIRS_TO_COPY" install.js` still returns 3 (definition + 2 usage sites -- no structural change)
    - The agents entry appears inside the DIRS_TO_COPY array brackets (between lines 13 and the closing `];`)
  </acceptance_criteria>
  <done>install.js DIRS_TO_COPY includes agents/ directory. npm and git-clone installs will copy agents/ttm-producer.md. Post-install validation will check for agents/ presence.</done>
</task>

<task type="auto">
  <name>Task 2: Add fix_needed and accepted to health.cjs validGateValues</name>
  <files>bin/lib/health.cjs</files>
  <read_first>
    - bin/lib/health.cjs (current state -- need to see validGateValues at line 252 and surrounding context)
  </read_first>
  <action>
In bin/lib/health.cjs at line 252, the current validGateValues set is:
```javascript
const validGateValues = new Set(['null', 'pass', 'warn', 'fail']);
```

Change to:
```javascript
const validGateValues = new Set(['null', 'pass', 'warn', 'fail', 'fix_needed', 'accepted']);
```

These two values are set by the deviation handling flow:
- `fix_needed` is set when user chooses "Correct" (option 1) in verify.md Step 6
- `accepted` is set when user chooses "Accept+log" (option 2) in verify.md Step 6 (line 372: `campaign update "${SLUG}" gate.[gate_field] "accepted"`)

Without these values in the allowlist, health.cjs reports false gate consistency failures for any campaign that has gone through deviation handling.
  </action>
  <verify>
    <automated>grep "validGateValues" bin/lib/health.cjs | grep -q "fix_needed" && grep "validGateValues" bin/lib/health.cjs | grep -q "accepted" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "validGateValues" bin/lib/health.cjs` includes both 'fix_needed' and 'accepted' in the Set constructor
    - The Set still includes the original 4 values: 'null', 'pass', 'warn', 'fail'
    - Total values in Set is 6
  </acceptance_criteria>
  <done>health.cjs gate consistency check recognizes fix_needed and accepted as valid gate values. No more false failures after deviation handling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| install.js source -> target filesystem | Copies directories from package to user's skill directory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02 | T (Tampering) | install.js directory copy | accept | Existing path traversal protection (path.resolve + homedir prefix check) already covers new agents/ directory -- no additional mitigation needed |
</threat_model>

<verification>
1. `grep "'agents'" install.js` -- matches inside DIRS_TO_COPY
2. `grep "fix_needed" bin/lib/health.cjs` -- matches inside validGateValues
3. `grep "accepted" bin/lib/health.cjs` -- matches inside validGateValues
</verification>

<success_criteria>
- npm/git-clone install copies agents/ directory to target and post-install validation checks for it
- health.cjs does not produce false gate consistency failures for campaigns with deviations
</success_criteria>

<output>
After completion, create `.planning/phases/11-gap-closure/11-02-SUMMARY.md`
</output>
