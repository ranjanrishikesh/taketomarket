---
phase: 11-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/lifecycle/learn.md
  - workflows/utility/archive.md
  - bin/lib/campaign.cjs
  - references/learnings-extraction.md
autonomous: true
gap_closure: true
requirements:
  - LRNG-01
  - LRNG-02
  - LRNG-03
  - LRNG-04
  - STAT-03
  - STAT-05

must_haves:
  truths:
    - "learn.md sets campaign phase to 'learned' via campaign update CLI after lesson extraction"
    - "archive accepts campaigns with phase='learned' in addition to phase='shipped'"
    - "learn.md, archive.md, and learnings-extraction.md reference VERIFICATION.md not VERIFY-REPORT-*.md"
  artifacts:
    - path: "workflows/lifecycle/learn.md"
      provides: "Phase transition to learned + correct verification filename"
      contains: "campaign update.*phase learned"
    - path: "workflows/utility/archive.md"
      provides: "Archive accepts learned campaigns"
      contains: "learned"
    - path: "bin/lib/campaign.cjs"
      provides: "CLI allows archiving learned campaigns"
      contains: "learned"
    - path: "references/learnings-extraction.md"
      provides: "Correct verification filename reference"
      contains: "VERIFICATION.md"
  key_links:
    - from: "workflows/lifecycle/learn.md"
      to: "bin/lib/campaign.cjs"
      via: "campaign update phase learned CLI call"
      pattern: "campaign update.*phase learned"
    - from: "workflows/utility/archive.md"
      to: "bin/lib/campaign.cjs"
      via: "cmdCampaignArchive phase check"
      pattern: "phase.*learned.*shipped"
---

<objective>
Fix the learn-to-archive lifecycle break (B-01) and the verification filename mismatch (B-03) so that the full campaign lifecycle (init through archive) completes end-to-end.

Purpose: These two blockers prevent the full campaign lifecycle from completing. B-01 means learn.md never transitions the campaign to phase=learned, and archive only accepts phase=shipped -- so post-learn campaigns are stuck. B-03 means learn.md, archive.md, and learnings-extraction.md reference VERIFY-REPORT-*.md which never exists (verify writes VERIFICATION.md), silently degrading lesson quality.

Output: Fixed learn.md, archive.md, campaign.cjs, and learnings-extraction.md enabling full init-to-archive lifecycle flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix learn.md -- add phase transition and fix VERIFY-REPORT references</name>
  <files>workflows/lifecycle/learn.md</files>
  <read_first>
    - workflows/lifecycle/learn.md (current state -- need to see exact line numbers and context for phase.learned and VERIFY-REPORT references)
    - workflows/lifecycle/measure.md (reference for how measure.md does phase transition -- pattern to follow)
  </read_first>
  <action>
Two changes to workflows/lifecycle/learn.md:

**Change 1 (B-01): Add campaign phase transition to "learned" in Step 8.**

In Step 8 ("Update Campaign State"), after the existing `campaign update ${SLUG} phase.learned true` line (line 353), add:
```bash
node "${CLAUDE_PLUGIN_ROOT}/bin/ttm-tools.cjs" campaign update ${SLUG} phase learned
```

This must appear BEFORE the `phase.learned true` timestamp line. The pattern follows the same convention as every other workflow (e.g., measure.md line 333: `campaign update ${SLUG} phase measured` followed by timestamp fields). The final Step 8 block should be:

```bash
node "${CLAUDE_PLUGIN_ROOT}/bin/ttm-tools.cjs" campaign update ${SLUG} phase learned
node "${CLAUDE_PLUGIN_ROOT}/bin/ttm-tools.cjs" campaign update ${SLUG} phase.learned true
node "${CLAUDE_PLUGIN_ROOT}/bin/ttm-tools.cjs" campaign update ${SLUG} learn.run_count ${RUN_COUNT}
...rest unchanged...
```

Wait -- review the existing line 353: it says `phase.learned true` which sets a boolean flag, NOT the phase field. The `phase learned` line (setting the phase field to "learned") is the missing one. Add it as the FIRST line in the Step 8 bash block.

**Change 2 (B-03): Fix all VERIFY-REPORT-*.md references to VERIFICATION.md.**

There are 4 locations in learn.md that reference VERIFY-REPORT:
1. Line 115: `.marketing/CAMPAIGNS/${SLUG}/VERIFY-REPORT-*.md` in the campaign artifacts loading list. Change to `.marketing/CAMPAIGNS/${SLUG}/VERIFICATION.md`
2. Line 185: "2. VERIFY-REPORT files for specific gate failure details" in the artifact scan order. Change to "2. VERIFICATION.md for specific gate failure details"
3. Line 395: checklist item "Campaign artifacts scanned in correct order (STATE > VERIFY-REPORT > FIX-BRIEF > REVIEW-FEEDBACK > BRIEF)". Change to "(STATE > VERIFICATION > FIX-BRIEF > REVIEW-FEEDBACK > BRIEF)"

Search for any other occurrences of "VERIFY-REPORT" in the file and change them all to "VERIFICATION.md" (or "VERIFICATION" when used as a label in ordered lists).
  </action>
  <verify>
    <automated>grep -c "VERIFY-REPORT" workflows/lifecycle/learn.md | grep -q "^0$" && grep -c "campaign update.*phase learned" workflows/lifecycle/learn.md | grep -q "^[1-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "VERIFY-REPORT" workflows/lifecycle/learn.md` returns 0
    - `grep -c "VERIFICATION.md" workflows/lifecycle/learn.md` returns at least 1
    - `grep "campaign update.*phase learned" workflows/lifecycle/learn.md` matches at least 1 line
    - `grep "campaign update.*phase.learned" workflows/lifecycle/learn.md` still exists (the timestamp line was not removed)
    - The `phase learned` line appears BEFORE the `phase.learned true` line in Step 8
  </acceptance_criteria>
  <done>learn.md sets phase=learned via campaign update CLI and references VERIFICATION.md instead of VERIFY-REPORT-*.md</done>
</task>

<task type="auto">
  <name>Task 2: Fix archive.md and campaign.cjs to accept phase=learned; fix archive.md VERIFY-REPORT reference</name>
  <files>workflows/utility/archive.md, bin/lib/campaign.cjs</files>
  <read_first>
    - workflows/utility/archive.md (current state -- need to see phase validation logic around line 103)
    - bin/lib/campaign.cjs (current state -- need to see cmdCampaignArchive phase check at line 411)
  </read_first>
  <action>
Three changes across two files:

**Change 1 (B-01): Update archive.md to accept phase=learned.**

In archive.md around lines 103-107, the current logic is:
```
- If `phase` does NOT equal `'shipped'`: tell user only shipped campaigns can be
  archived. Display current phase and suggest completing remaining phases:
  "Campaign '${SLUG}' is in phase '${phase}'. Only shipped campaigns can be
  archived. Complete the remaining phases first."
  Exit.
```

Change to accept both 'shipped' and 'learned':
```
- If `phase` is NOT one of `'shipped'` or `'learned'`: tell user only shipped or learned campaigns can be
  archived. Display current phase and suggest completing remaining phases:
  "Campaign '${SLUG}' is in phase '${phase}'. Only shipped or learned campaigns can be
  archived. Complete the remaining phases first."
  Exit.
```

**Change 2 (B-01): Update campaign.cjs cmdCampaignArchive to accept phase=learned.**

In bin/lib/campaign.cjs at line 411, the current check is:
```javascript
if (frontmatter.phase !== 'shipped') {
    error('Only shipped campaigns can be archived. Current phase: ' + frontmatter.phase);
}
```

Change to:
```javascript
if (frontmatter.phase !== 'shipped' && frontmatter.phase !== 'learned') {
    error('Only shipped or learned campaigns can be archived. Current phase: ' + frontmatter.phase);
}
```

Also update the JSDoc comment at line 361:
```javascript
 * Only campaigns with phase='shipped' or phase='learned' can be archived.
```

**Change 3 (B-03): Fix VERIFY-REPORT reference in archive.md.**

In archive.md line 124:
```
4. Any `VERIFY-REPORT-*.md` files in the campaign directory (gate details)
```
Change to:
```
4. `VERIFICATION.md` in the campaign directory (gate details)
```
  </action>
  <verify>
    <automated>grep -c "VERIFY-REPORT" workflows/utility/archive.md | grep -q "^0$" && grep "learned" bin/lib/campaign.cjs | grep -q "learned" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "VERIFY-REPORT" workflows/utility/archive.md` returns 0
    - `grep "VERIFICATION.md" workflows/utility/archive.md` returns at least 1 match
    - `grep "learned" bin/lib/campaign.cjs` shows the phase check accepts 'learned'
    - `grep "frontmatter.phase !== 'shipped' && frontmatter.phase !== 'learned'" bin/lib/campaign.cjs` returns 1 match
    - `grep "'shipped' or 'learned'" workflows/utility/archive.md` returns at least 1 match
  </acceptance_criteria>
  <done>Archive workflow accepts both shipped and learned campaigns. VERIFY-REPORT reference replaced with VERIFICATION.md. Full campaign lifecycle (init through learn through archive) can now complete.</done>
</task>

<task type="auto">
  <name>Task 3: Fix learnings-extraction.md VERIFY-REPORT references</name>
  <files>references/learnings-extraction.md</files>
  <read_first>
    - references/learnings-extraction.md (current state -- need to see all VERIFY-REPORT references)
  </read_first>
  <action>
Fix all VERIFY-REPORT-*.md references in references/learnings-extraction.md to VERIFICATION.md:

1. Line 24: `check VERIFY-REPORT-*.md for the specific failure details` -> `check VERIFICATION.md for the specific failure details`
2. Line 89: `| \`VERIFY-REPORT-*.md\` | Gate check details, pass/fail specifics, deviation justifications |` -> `| \`VERIFICATION.md\` | Gate check details, pass/fail specifics, deviation justifications |`
3. Line 94: `then VERIFY-REPORT and FIX-BRIEF (failure details)` -> `then VERIFICATION.md and FIX-BRIEF (failure details)`

Search the entire file for any other occurrences and fix them all.
  </action>
  <verify>
    <automated>grep -c "VERIFY-REPORT" references/learnings-extraction.md | grep -q "^0$" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "VERIFY-REPORT" references/learnings-extraction.md` returns 0
    - `grep -c "VERIFICATION.md" references/learnings-extraction.md` returns at least 2
  </acceptance_criteria>
  <done>learnings-extraction.md references VERIFICATION.md consistently, matching the actual filename written by the verify workflow</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args -> campaign.cjs | Phase value from STATE.md frontmatter checked against allowlist |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | T (Tampering) | campaign.cjs archive phase check | mitigate | Explicit allowlist check: phase must equal 'shipped' OR 'learned' -- no wildcard or regex |
</threat_model>

<verification>
1. `grep -c "VERIFY-REPORT" workflows/lifecycle/learn.md workflows/utility/archive.md references/learnings-extraction.md` -- all return 0
2. `grep "campaign update.*phase learned" workflows/lifecycle/learn.md` -- returns at least 1 line
3. `grep "frontmatter.phase !== 'shipped' && frontmatter.phase !== 'learned'" bin/lib/campaign.cjs` -- returns 1 match
4. `grep "'shipped' or 'learned'" workflows/utility/archive.md` -- returns at least 1 match
</verification>

<success_criteria>
- Full campaign lifecycle flow (init -> ... -> learn -> archive) is unbroken -- learn.md transitions phase to "learned" and archive accepts "learned" campaigns
- All three files (learn.md, archive.md, learnings-extraction.md) reference VERIFICATION.md, not VERIFY-REPORT-*.md
- No regression: archive still accepts phase=shipped campaigns
</success_criteria>

<output>
After completion, create `.planning/phases/11-gap-closure/11-01-SUMMARY.md`
</output>
