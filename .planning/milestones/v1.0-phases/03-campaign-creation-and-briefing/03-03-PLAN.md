---
phase: 03-campaign-creation-and-briefing
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - workflows/lifecycle/brief.md
  - workflows/lifecycle/brief-positioning-check.md
  - skills/ttm-brief/SKILL.md
autonomous: true
requirements: [LIFE-03, LIFE-04, LIFE-05]

must_haves:
  truths:
    - "User runs /ttm-brief and receives a campaign brief with all mandatory fields: goal, outcome metric, target value, measurement window, ICP segment, positioning anchor, hook, proof points, channel mix, assets list"
    - "Brief refuses to complete without an outcome metric after 2 re-prompt retries"
    - "Brief runs positioning check gate and inserts visible drift warning when positioning misalignment detected"
    - "Brief warns when output metric is missing but does not block"
    - "Brief warns when research has not been run but allows user to proceed (per D-08)"
  artifacts:
    - path: "workflows/lifecycle/brief.md"
      provides: "Brief generation workflow with outcome enforcement and positioning gate"
      contains: "outcome_metric"
    - path: "workflows/lifecycle/brief-positioning-check.md"
      provides: "Positioning check rules reference file"
      contains: "POSITIONING DRIFT WARNING"
    - path: "skills/ttm-brief/SKILL.md"
      provides: "Skill entry point routing to brief.md workflow"
  key_links:
    - from: "skills/ttm-brief/SKILL.md"
      to: "workflows/lifecycle/brief.md"
      via: "Read and follow the workflow"
      pattern: "workflows/lifecycle/brief\\.md"
    - from: "workflows/lifecycle/brief.md"
      to: "workflows/lifecycle/brief-positioning-check.md"
      via: "@-syntax reference for positioning gate rules"
      pattern: "brief-positioning-check\\.md"
    - from: "workflows/lifecycle/brief.md"
      to: ".marketing/POSITIONING.md"
      via: "Read Tier 1 summary for positioning anchor and drift check"
      pattern: "POSITIONING\\.md"
    - from: "workflows/lifecycle/brief.md"
      to: ".marketing/CAMPAIGNS/<slug>/BRIEF.md"
      via: "Writes completed brief from template"
      pattern: "BRIEF\\.md"
---

<objective>
Build the /ttm-brief workflow with outcome metric enforcement (LIFE-04) and positioning check gate (LIFE-05) -- the final command in the Phase 3 campaign creation flow.

Purpose: The brief is the contract between the user and the production system. It must contain all mandatory fields (D-07), enforce outcome metrics with guided re-prompting (D-06), and run a positioning check gate that warns on drift without blocking (D-05). This is the gate between campaign planning and content production.

Output: brief.md workflow, brief-positioning-check.md reference file, updated SKILL.md entry point.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-campaign-creation-and-briefing/03-RESEARCH.md
@.planning/phases/03-campaign-creation-and-briefing/03-PATTERNS.md
@.planning/phases/03-campaign-creation-and-briefing/03-CONTEXT.md
@.planning/phases/03-campaign-creation-and-briefing/03-01-SUMMARY.md

<interfaces>
<!-- Workflow structural pattern from Phase 2 init.md -->

Every workflow file uses these five top-level XML tags:
```xml
<purpose> ... </purpose>
<required_reading> @${CLAUDE_PLUGIN_ROOT}/... </required_reading>
<process> [Numbered steps] </process>
<success_criteria> - [ ] ... </success_criteria>
<output> - `path/to/file` </output>
```

CLI commands available after Plan 01:
```bash
node ${CLAUDE_PLUGIN_ROOT}/bin/ttm-tools.cjs campaign state <slug> --raw
node ${CLAUDE_PLUGIN_ROOT}/bin/ttm-tools.cjs campaign update <slug> <field> <value>
node ${CLAUDE_PLUGIN_ROOT}/bin/ttm-tools.cjs timestamp --raw
```

Text-mode detection pattern (from init.md lines 17-39):
```markdown
**Text mode:** Set TEXT_MODE=true if --text is present in $ARGUMENTS
or if the runtime is not Claude Code. Replace each AskUserQuestion
call with a plain-text numbered list.
```

Existing campaign brief template (templates/campaign-brief.md):
- Has [GENERATED BY /ttm-brief] placeholders for all fields
- Sections: Campaign Name, Goal, Outcome Metric, Output Metric, ICP Segment,
  Positioning Anchor, Hook, Proof Points, Channel Mix, Assets List,
  Success Criteria, Failure Criteria, Dependencies, Timeline

POSITIONING.md summary structure (from templates/reference-files/positioning.md):
- Primary differentiator phrase
- Category
- Target audience
- Must-include proof points
- Must-not-say terms
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create workflows/lifecycle/brief-positioning-check.md reference file</name>
  <files>workflows/lifecycle/brief-positioning-check.md</files>
  <read_first>
    workflows/setup/init-validation.md
    gates/base-gates.md
    templates/reference-files/positioning.md
    .planning/phases/03-campaign-creation-and-briefing/03-PATTERNS.md
  </read_first>
  <action>
Create `workflows/lifecycle/brief-positioning-check.md` as a reference file (NOT a workflow). This is referenced by brief.md via @-syntax to externalize the positioning check rules, keeping brief.md under 500 lines. Follow the structure of `workflows/setup/init-validation.md`.

The file defines 5 positioning checks applied to a completed brief:

```markdown
# Positioning Check Rules

## Usage

Referenced by `workflows/lifecycle/brief.md` via @-syntax.
Applied after brief content is fully generated to detect positioning drift.
This is a soft gate per D-05 -- the brief is ALWAYS generated, drift produces a warning, not a block.

---

## Check 1: Positioning Anchor Alignment

**Field:** Brief's "Positioning Anchor > Key message"
**Checks against:** POSITIONING.md primary differentiator phrase

- PASS: Brief's positioning anchor restates or naturally extends the primary differentiator
- WARN: Brief's anchor partially overlaps but introduces claims not present in POSITIONING.md
- FAIL: Brief's anchor uses a different differentiation claim entirely

**Drift detail format:** "Positioning anchor '[brief anchor]' does not align with primary differentiator '[positioning differentiator]'"

## Check 2: ICP Segment Match

**Field:** Brief's "ICP Segment > Primary segment"
**Checks against:** POSITIONING.md target audience

- PASS: Brief's ICP segment matches or is a sub-segment of the POSITIONING.md target audience
- WARN: Brief targets an adjacent audience not explicitly in POSITIONING.md
- FAIL: Brief targets a completely different audience

**Drift detail format:** "ICP segment '[brief segment]' does not match target audience '[positioning audience]'"

## Check 3: Proof Point Sourcing

**Field:** Brief's "Proof Points" table
**Checks against:** POSITIONING.md must-include proof points

- PASS: All proof points in the brief are sourced from POSITIONING.md proof point library
- WARN: Brief includes proof points not in the library (may be valid new evidence)
- FAIL: Brief makes claims with no proof point sourcing at all

**Drift detail format:** "Proof point '[claim]' not found in POSITIONING.md proof point library"

## Check 4: Must-Not-Say Terms

**Field:** Entire brief content
**Checks against:** POSITIONING.md must-not-say terms list

- PASS: No must-not-say terms found in brief
- FAIL: One or more must-not-say terms detected

**Drift detail format:** "Must-not-say term '[term]' found in brief [section]"

## Check 5: Hook-Positioning Coherence

**Field:** Brief's "Hook" section
**Checks against:** POSITIONING.md primary differentiator and category

- PASS: Hook reinforces the positioning -- uses aligned language and framing
- WARN: Hook is neutral -- does not reinforce but does not contradict
- FAIL: Hook contradicts the positioning or uses competitor framing

**Drift detail format:** "Hook framing '[hook summary]' contradicts positioning category '[category]'"

---

## Gate Result Logic

- If ALL checks PASS: gate result = "pass"
- If ANY check is WARN or FAIL: gate result = "warn"
- The brief is ALWAYS generated regardless of gate result (per D-05)

## Drift Warning Template

When gate result is "warn", insert at the TOP of BRIEF.md after the title line:

<!--
!! POSITIONING DRIFT WARNING !!
The following items may not align with your positioning:
- [drift detail from each failing check]
Review .marketing/POSITIONING.md and adjust the brief if needed.
Run /ttm-positioning-check for a full audit.
-->

This is an HTML comment so it is visible in source but not rendered.
```

Keep under 150 lines.
  </action>
  <verify>
    <automated>cd /Users/rishikeshranjan/code/rishiPersonal/takeToMarket && grep -q "Check 1: Positioning Anchor" workflows/lifecycle/brief-positioning-check.md && grep -q "Check 2: ICP Segment" workflows/lifecycle/brief-positioning-check.md && grep -q "Check 3: Proof Point" workflows/lifecycle/brief-positioning-check.md && grep -q "Check 4: Must-Not-Say" workflows/lifecycle/brief-positioning-check.md && grep -q "Check 5: Hook" workflows/lifecycle/brief-positioning-check.md && grep -q "POSITIONING DRIFT WARNING" workflows/lifecycle/brief-positioning-check.md && grep -q "D-05" workflows/lifecycle/brief-positioning-check.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "Check 1: Positioning Anchor" workflows/lifecycle/brief-positioning-check.md
    - grep -q "Check 2: ICP Segment" workflows/lifecycle/brief-positioning-check.md
    - grep -q "Check 3: Proof Point" workflows/lifecycle/brief-positioning-check.md
    - grep -q "Check 4: Must-Not-Say" workflows/lifecycle/brief-positioning-check.md
    - grep -q "Check 5: Hook" workflows/lifecycle/brief-positioning-check.md
    - grep -q "POSITIONING DRIFT WARNING" workflows/lifecycle/brief-positioning-check.md
    - grep -q "ALWAYS generated" workflows/lifecycle/brief-positioning-check.md (soft gate per D-05)
    - grep -q "PASS" workflows/lifecycle/brief-positioning-check.md (pass criteria defined)
    - grep -q "WARN" workflows/lifecycle/brief-positioning-check.md (warn criteria defined)
    - grep -q "FAIL" workflows/lifecycle/brief-positioning-check.md (fail criteria defined)
  </acceptance_criteria>
  <done>brief-positioning-check.md defines 5 positioning checks with PASS/WARN/FAIL criteria, drift detail format strings, gate result logic, and drift warning HTML comment template. Explicitly documents soft-gate behavior per D-05.</done>
</task>

<task type="auto">
  <name>Task 2: Create workflows/lifecycle/brief.md workflow and update skills/ttm-brief/SKILL.md</name>
  <files>workflows/lifecycle/brief.md, skills/ttm-brief/SKILL.md</files>
  <read_first>
    workflows/setup/init.md
    workflows/lifecycle/brief-positioning-check.md
    skills/ttm-brief/SKILL.md
    templates/campaign-brief.md
    references/context-loading.md
    .planning/phases/03-campaign-creation-and-briefing/03-PATTERNS.md
    .planning/phases/03-campaign-creation-and-briefing/03-CONTEXT.md
  </read_first>
  <action>
Create `workflows/lifecycle/brief.md` following the init.md structural pattern. This is the most complex workflow in Phase 3 -- it generates the campaign brief with outcome metric enforcement (D-06) and positioning check gate (D-05). Use XML tags: purpose, required_reading, process, success_criteria, output.

In required_reading, include:
- `@${CLAUDE_PLUGIN_ROOT}/references/context-loading.md`
- `@${CLAUDE_PLUGIN_ROOT}/workflows/lifecycle/brief-positioning-check.md`
- `@${CLAUDE_PLUGIN_ROOT}/templates/campaign-brief.md`

The workflow has 8 steps:

**Step 1: Load context**
- Text-mode detection: set TEXT_MODE=true if --text is in $ARGUMENTS or runtime is not Claude Code
- Read Tier 1 summaries from all 9 .marketing/ reference files
- Read Tier 2 (full content) for: `.marketing/ICP.md`, `.marketing/CHANNELS.md`, `.marketing/METRICS.md`, `.marketing/CALENDAR.md` (per context-loading.md loading matrix)
- Read full `.marketing/POSITIONING.md` (needed for positioning check gate)
- Read campaign files: `.marketing/CAMPAIGNS/${SLUG}/STATE.md`, `.marketing/CAMPAIGNS/${SLUG}/RESEARCH.md`
- Display stage banner: `takeToMarket > LOADING CONTEXT`

**Step 2: Validate campaign and phase order (per D-08)**
- Extract slug from $ARGUMENTS (strip --text flag)
- Validate campaign exists (check STATE.md)
- Read campaign state
- If phase is "created" (not "researched"): warn: "Research not yet completed for this campaign. Brief quality will be limited without research data. Run /ttm-research ${SLUG} first for better results. Proceed without research?"
- Wait for user confirmation if warning triggered
- Check if RESEARCH.md has actual content (not just template placeholders). If template-only: note RESEARCH_AVAILABLE=false

**Step 3: Outcome metric enforcement (per D-06, LIFE-04)**
- Display stage banner: `takeToMarket > GENERATING BRIEF`
- Ask (freeform, NOT AskUserQuestion): "What is the goal of this campaign? (One sentence describing the campaign's purpose)"
- Store as CAMPAIGN_GOAL
- Ask (freeform): "What is the outcome metric for this campaign? An outcome metric measures a business result, not an output action.\n\nExamples:\n  - VALID: 'Increase trial signups by 20% within 30 days'\n  - VALID: 'Generate 50 qualified leads from organic search in Q2'\n  - NOT VALID: 'Publish 4 blog posts' (this is an output, not an outcome)\n\nWhat business outcome will this campaign drive?"
- Validate the response:
  - FAIL if metric starts with output verbs: publish, write, send, create, post, design, build, launch (without a measurable outcome)
  - FAIL if no target value (number, percentage, or specific benchmark) is present
  - FAIL if no measurement window (timeframe) is present
- On FAIL (retry 1): re-prompt with explanation: "That looks like an output metric, not an outcome. An output is what you produce (blog posts, emails). An outcome is the business result (signups, revenue, leads). Please provide: [specific business outcome] + [target value] + [measurement window]"
- On FAIL (retry 2): re-prompt once more with different framing
- After 2 failed retries: block brief generation. Tell user: "Cannot generate a brief without a valid outcome metric. A brief without a measurable business outcome would produce untestable content. Run /ttm-brief ${SLUG} again when ready." Exit.
- On PASS: store as OUTCOME_METRIC. Extract target_value and measurement_window from the response.
- Ask (freeform): "What are the output metrics? (Assets and volume to produce, e.g., '4 blog posts, 2 emails, 1 landing page')"
- If empty response: set OUTPUT_METRIC_MISSING=true. Continue. This is allowed per D-06.

**Step 4: Collect remaining brief fields (per D-07)**
- ICP Segment: suggest from ICP.md primary segment. Ask if correct or if targeting a sub-segment.
- Channel Mix: use AskUserQuestion (or text-mode numbered list) with multiSelect=true. Options derived from CHANNELS.md active channels. Ask role per selected channel (primary/support/amplification).
- Hook: ask user for the opening angle or attention-capture strategy. Suggest options based on RESEARCH.md findings if RESEARCH_AVAILABLE=true.
- Additional proof points: show proof points from POSITIONING.md. Ask if user wants to add campaign-specific claims.
- Timeline: ask for target ship date. Derive remaining timeline dates backward.
- Success criteria: ask what specific, measurable outcomes define success beyond the outcome metric.
- Failure criteria: ask what conditions should trigger a campaign pause or kill.

**Step 5: Generate BRIEF.md**
- Read the brief template: `${CLAUDE_PLUGIN_ROOT}/templates/campaign-brief.md`
- Fill ALL `[GENERATED BY /ttm-brief]` placeholders with collected data:
  - Campaign Name from campaign STATE.md name field
  - Goal from CAMPAIGN_GOAL
  - Outcome Metric section: metric, target_value, measurement_window, data source
  - Output Metric section: from output metrics or flag if missing
  - ICP Segment: from Step 4
  - Positioning Anchor: key message derived from POSITIONING.md primary differentiator, primary differentiator quoted from POSITIONING.md
  - Hook: from Step 4
  - Proof Points table: from POSITIONING.md proof library + any campaign-specific additions
  - Channel Mix table: from Step 4 channel selection with roles
  - Assets List table: derived from output metrics + channel mix
  - Success/Failure Criteria: from Step 4
  - Dependencies: inferred from campaign context
  - Timeline table: from Step 4
- If OUTPUT_METRIC_MISSING=true, insert after the Output Metric section:
  `<!-- OUTPUT_METRIC_MISSING: Add output metrics before /ttm-produce -->`
- Store generated content as BRIEF_CONTENT

**Step 6: Positioning check gate (per D-05, LIFE-05)**
- Display stage banner: `takeToMarket > POSITIONING CHECK`
- Apply all 5 checks from brief-positioning-check.md against BRIEF_CONTENT:
  1. Positioning anchor alignment vs POSITIONING.md differentiator
  2. ICP segment match vs POSITIONING.md target audience
  3. Proof point sourcing vs POSITIONING.md proof library
  4. Must-not-say term scan across entire brief
  5. Hook-positioning coherence vs POSITIONING.md framing
- Determine gate result: "pass" if all checks pass, "warn" if any WARN or FAIL
- If gate result is "warn": insert drift warning HTML comment block at top of BRIEF_CONTENT (after the title), listing each specific drift item with the drift detail format string from the check rules
- Write BRIEF.md unconditionally (per D-05 -- NEVER block on positioning drift)

**Step 7: Write BRIEF.md and update state**
- Write BRIEF_CONTENT to `.marketing/CAMPAIGNS/${SLUG}/BRIEF.md`
- Update campaign state:
  ```bash
  node ${CLAUDE_PLUGIN_ROOT}/bin/ttm-tools.cjs campaign update ${SLUG} phase briefed
  node ${CLAUDE_PLUGIN_ROOT}/bin/ttm-tools.cjs campaign update ${SLUG} phase.briefed $(node ${CLAUDE_PLUGIN_ROOT}/bin/ttm-tools.cjs timestamp --raw)
  node ${CLAUDE_PLUGIN_ROOT}/bin/ttm-tools.cjs campaign update ${SLUG} gate.outcome_metric pass
  node ${CLAUDE_PLUGIN_ROOT}/bin/ttm-tools.cjs campaign update ${SLUG} gate.positioning_check [pass|warn]
  ```

**Step 8: Display summary**
- Display:
  ```
  takeToMarket > BRIEF COMPLETE

  Brief saved to .marketing/CAMPAIGNS/${SLUG}/BRIEF.md

  Outcome metric: [OUTCOME_METRIC]
  Positioning check: [pass|warn]
  Output metric: [defined|MISSING -- add before /ttm-produce]
  Channels: [list of selected channels]
  Assets: [count] planned

  [If positioning_check=warn:]
  !! Positioning drift detected -- see warning in BRIEF.md
  Review .marketing/POSITIONING.md and adjust if needed.

  Next: Run /ttm-produce ${SLUG}
  ```

Success criteria:
- [ ] Context loaded (Tier 1 all files, Tier 2 ICP/CHANNELS/METRICS/CALENDAR, full POSITIONING.md)
- [ ] Campaign validated and phase order checked
- [ ] Outcome metric enforced -- valid business outcome with target value and measurement window
- [ ] All mandatory brief fields present in generated BRIEF.md
- [ ] Positioning check gate executed with specific drift items listed
- [ ] BRIEF.md written unconditionally (soft gate per D-05)
- [ ] Campaign state updated: phase=briefed, gate fields set
- [ ] Output metric missing flag visible if applicable

Output:
- `.marketing/CAMPAIGNS/<slug>/BRIEF.md` (populated with all mandatory fields)

Keep the workflow under 500 lines. The heavy positioning check logic is externalized to brief-positioning-check.md via @-syntax.

Then update `skills/ttm-brief/SKILL.md`:
- Remove the "**Status:** Not yet implemented (Phase 3)" line
- Remove the "This command will:" bullet list
- Keep existing frontmatter name, description, argument-hint, disable-model-invocation: true
- Update allowed-tools to: `Read Write Bash Glob Grep AskUserQuestion` (add AskUserQuestion for channel selection)
- Body: `Read and follow the workflow at \`${CLAUDE_PLUGIN_ROOT}/workflows/lifecycle/brief.md\``
  </action>
  <verify>
    <automated>cd /Users/rishikeshranjan/code/rishiPersonal/takeToMarket && grep -q "<purpose>" workflows/lifecycle/brief.md && grep -q "<process>" workflows/lifecycle/brief.md && grep -q "outcome_metric\|outcome metric\|OUTCOME_METRIC" workflows/lifecycle/brief.md && grep -q "positioning_check\|positioning check\|POSITIONING CHECK" workflows/lifecycle/brief.md && grep -q "brief-positioning-check.md" workflows/lifecycle/brief.md && grep -q "OUTPUT_METRIC_MISSING" workflows/lifecycle/brief.md && grep -q "campaign update" workflows/lifecycle/brief.md && grep -q "gate.outcome_metric" workflows/lifecycle/brief.md && grep -q "gate.positioning_check" workflows/lifecycle/brief.md && ! grep -q "Not yet implemented" skills/ttm-brief/SKILL.md && grep -q "AskUserQuestion" skills/ttm-brief/SKILL.md && grep -q "brief.md" skills/ttm-brief/SKILL.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "<purpose>" workflows/lifecycle/brief.md
    - grep -q "<process>" workflows/lifecycle/brief.md
    - grep -q "<success_criteria>" workflows/lifecycle/brief.md
    - grep -q "outcome" workflows/lifecycle/brief.md (outcome metric enforcement per D-06)
    - grep -q "output.*metric\|OUTPUT_METRIC" workflows/lifecycle/brief.md (output metric warning per D-06)
    - grep -q "brief-positioning-check.md" workflows/lifecycle/brief.md (@-reference to check rules)
    - grep -q "POSITIONING DRIFT WARNING\|positioning.*drift\|positioning.*check" workflows/lifecycle/brief.md (positioning gate per D-05)
    - grep -q "gate.outcome_metric" workflows/lifecycle/brief.md (state update)
    - grep -q "gate.positioning_check" workflows/lifecycle/brief.md (state update)
    - grep -q "campaign update.*phase briefed" workflows/lifecycle/brief.md (phase transition)
    - grep -q "ICP" workflows/lifecycle/brief.md (ICP segment field per D-07)
    - grep -q "Channel\|channel" workflows/lifecycle/brief.md (channel mix per D-07)
    - grep -q "AskUserQuestion" skills/ttm-brief/SKILL.md (allowed-tools updated)
    - ! grep -q "Not yet implemented" skills/ttm-brief/SKILL.md (stub removed)
    - wc -l < workflows/lifecycle/brief.md | awk '{exit ($1 > 500)}' (under 500 lines)
  </acceptance_criteria>
  <done>brief.md workflow collects all mandatory fields per D-07, enforces outcome metric with 2-retry cap per D-06/LIFE-04, runs 5-check positioning gate per D-05/LIFE-05, generates complete BRIEF.md from template, and updates campaign state with phase and gate results. SKILL.md updated with AskUserQuestion in allowed-tools.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input -> brief fields | Freeform text from user becomes brief content |
| POSITIONING.md -> positioning check | Reference file content drives gate decisions |
| Template -> BRIEF.md | Template placeholders replaced with user + reference data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering | brief.md campaign state updates | mitigate | Uses campaign CLI which re-sanitizes slug and validates path |
| T-03-08 | Repudiation | brief.md positioning check gate | mitigate | Gate result logged in campaign STATE.md (gate.positioning_check field) for audit trail per D-09 |
| T-03-09 | Information Disclosure | brief.md loads multiple reference files | accept | All local files, single-user, no network exposure |
</threat_model>

<verification>
- brief.md follows init.md XML structural pattern with all 5 tags
- Outcome metric enforcement re-prompts twice then blocks (per D-06)
- Positioning check uses all 5 checks from brief-positioning-check.md
- Brief is ALWAYS written even on positioning drift (per D-05)
- All mandatory fields from D-07 present in generated BRIEF.md
- Campaign STATE.md updated with phase=briefed and both gate results
- SKILL.md has AskUserQuestion in allowed-tools
</verification>

<success_criteria>
- /ttm-brief generates complete brief with all mandatory fields from D-07
- Outcome metric enforcement blocks without valid business outcome after 2 retries
- Positioning check gate generates warning on drift without blocking brief generation
- Campaign state records both gate results for downstream audit
- Output metric missing flag visible when user skips output metric
</success_criteria>

<output>
After completion, create `.planning/phases/03-campaign-creation-and-briefing/03-03-SUMMARY.md`
</output>
