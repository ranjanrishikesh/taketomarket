---
phase: 03-campaign-creation-and-briefing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/lib/campaign.cjs
  - bin/ttm-tools.cjs
  - templates/campaign-state.md
  - templates/campaign-research.md
autonomous: true
requirements: [LIFE-01]

must_haves:
  truths:
    - "ttm-tools.cjs campaign init <slug> <name> creates campaign directory with STATE.md, RESEARCH.md placeholder, BRIEF.md placeholder, and ASSETS/ directory"
    - "ttm-tools.cjs campaign state <slug> reads and returns campaign STATE.md frontmatter as JSON"
    - "ttm-tools.cjs campaign update <slug> <field> <value> atomically updates a single field in campaign STATE.md"
    - "Campaign STATE.md uses flat dot-notation frontmatter compatible with parseFrontmatter()"
  artifacts:
    - path: "bin/lib/campaign.cjs"
      provides: "Campaign state CRUD operations"
      exports: ["cmdCampaignInit", "cmdCampaignState", "cmdCampaignUpdate"]
    - path: "templates/campaign-state.md"
      provides: "Per-campaign STATE.md template with flat frontmatter"
      contains: "phase.created"
    - path: "templates/campaign-research.md"
      provides: "Per-campaign RESEARCH.md template with confidence score tables"
      contains: "## Market Context"
  key_links:
    - from: "bin/ttm-tools.cjs"
      to: "bin/lib/campaign.cjs"
      via: "require('./lib/campaign.cjs') in campaign case"
      pattern: "case 'campaign'"
    - from: "bin/lib/campaign.cjs"
      to: "bin/lib/core.cjs"
      via: "require('./core.cjs') for parseFrontmatter, safeWriteFile"
      pattern: "require.*core\\.cjs"
---

<objective>
Build the campaign CLI utility and templates that all three Phase 3 workflows depend on.

Purpose: Campaign state operations (init, read, update) must be deterministic CLI operations per Phase 1 D-12 pattern. Templates define the scaffold structure that /ttm-new-campaign creates. This plan provides the foundation for Plans 02 and 03.

Output: bin/lib/campaign.cjs module, ttm-tools.cjs campaign subcommand, campaign-state.md and campaign-research.md templates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-campaign-creation-and-briefing/03-RESEARCH.md
@.planning/phases/03-campaign-creation-and-briefing/03-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs from existing codebase -->

From bin/lib/core.cjs (verified exports):
```javascript
module.exports = {
  output,          // (data, raw, rawStr) => void -- prints JSON or raw string
  error,           // (msg) => process.exit(1) with error message
  safeReadFile,    // (filePath) => string|null -- returns null if not found
  safeWriteFile,   // (filePath, content) => void -- creates parent dirs automatically
  parseFrontmatter,    // (content) => { frontmatter: {}, body: string } -- flat key:value only
  serializeFrontmatter, // (frontmatter, body) => string -- auto-quotes values with : or #
  parseNamedArgs,  // (args) => { positional: [], named: {} }
};
```

From bin/lib/state.cjs (pattern to follow exactly):
```javascript
function resolveStatePath() {
  const statePath = path.resolve(process.cwd(), '.marketing', 'STATE.md');
  const projectRoot = path.resolve(process.cwd());
  if (!statePath.startsWith(projectRoot)) {
    error('STATE.md path escapes project directory');
  }
  return statePath;
}

function cmdStateRead(raw) { /* parse frontmatter, output JSON */ }
function cmdStateUpdate(field, value, raw) { /* update single field, set last_updated */ }

module.exports = { cmdStateRead, cmdStateUpdate };
```

From bin/ttm-tools.cjs (router pattern):
```javascript
switch (command) {
  case 'slug': { /* ... */ break; }
  case 'state': { /* ... */ break; }
  // New 'campaign' case goes here, before default
  default: error(`Unknown command: ${command || '(none)'}. Available: ...`);
}
```

From bin/lib/slug.cjs:
```javascript
// slug sanitization: text.toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, '').substring(0, 60)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create bin/lib/campaign.cjs with init/state/update operations and add campaign subcommand to ttm-tools.cjs</name>
  <files>bin/lib/campaign.cjs, bin/ttm-tools.cjs</files>
  <read_first>
    bin/lib/state.cjs
    bin/lib/core.cjs
    bin/ttm-tools.cjs
    bin/lib/slug.cjs
  </read_first>
  <action>
Create `bin/lib/campaign.cjs` following the exact structure of `bin/lib/state.cjs`:

1. Module header: JSDoc comment describing purpose, zero deps, depends on core.cjs. 'use strict'. Require path and core.cjs exports (output, error, safeReadFile, safeWriteFile, parseFrontmatter, serializeFrontmatter).

2. `resolveCampaignStatePath(slug)`:
   - If !slug or !slug.trim(), call error('campaign slug required')
   - Re-sanitize slug: `slug.toLowerCase().replace(/[^a-z0-9-]/g, '')` (defense in depth per D-10 and PATTERNS.md Path Security)
   - Resolve path: `path.resolve(process.cwd(), '.marketing', 'CAMPAIGNS', safe, 'STATE.md')`
   - Security check: verify resolved path starts with `path.resolve(process.cwd())`
   - Return the path

3. `cmdCampaignInit(slug, name, raw)`:
   - Validate slug and name are non-empty
   - Re-sanitize slug same as resolveCampaignStatePath
   - Build paths: campaignDir, assetsDir, statePath
   - Security check: campaignDir must startsWith project root
   - `fs.mkdirSync(assetsDir, { recursive: true })` -- creates CAMPAIGNS/<slug>/ASSETS/
   - Generate timestamp via `new Date().toISOString()`
   - Build STATE.md content with flat dot-notation frontmatter:
     ```
     ---
     campaign: <slug>
     name: <name>
     created: <timestamp>
     phase: created
     last_updated: <timestamp>
     phase.created: <timestamp>
     phase.researched: null
     phase.briefed: null
     phase.produced: null
     phase.verified: null
     phase.reviewed: null
     phase.fixed: null
     phase.shipped: null
     phase.measured: null
     phase.learned: null
     gate.positioning_check: null
     gate.outcome_metric: null
     ---

     # Campaign: <name>

     Phase: created
     Next step: Run `/ttm-research <slug>` to gather market intelligence.
     ```
   - Write STATE.md via safeWriteFile
   - Copy RESEARCH.md template: read `templates/campaign-research.md` from CLAUDE_PLUGIN_ROOT or fallback to relative path `path.resolve(__dirname, '..', '..', 'templates', 'campaign-research.md')`. Replace `[SLUG]` with slug, `[CAMPAIGN_NAME]` with name. Write to `CAMPAIGNS/<slug>/RESEARCH.md` via safeWriteFile.
   - Copy BRIEF.md template: read `templates/campaign-brief.md` from same relative path pattern. Write to `CAMPAIGNS/<slug>/BRIEF.md` via safeWriteFile.
   - Output: `{ created: true, slug, name, path: statePath }`

4. `cmdCampaignState(slug, raw)`:
   - Call resolveCampaignStatePath(slug)
   - safeReadFile the path
   - If null: output `{ exists: false, error: 'Campaign STATE.md not found for slug: <slug>' }`
   - If found: parseFrontmatter, output `{ exists: true, ...frontmatter, body_preview: body.substring(0, 200) }`

5. `cmdCampaignUpdate(slug, field, value, raw)`:
   - Validate field and value are present
   - resolveCampaignStatePath(slug)
   - safeReadFile, error if null
   - parseFrontmatter, update field, set last_updated to new timestamp
   - serializeFrontmatter and safeWriteFile
   - Output: `{ updated: field, value, last_updated: timestamp }`

6. Export: `{ cmdCampaignInit, cmdCampaignState, cmdCampaignUpdate }`

Then update `bin/ttm-tools.cjs`:
- Add `case 'campaign':` block before the `default:` case
- Require campaign.cjs: `const { cmdCampaignInit, cmdCampaignState, cmdCampaignUpdate } = require('./lib/campaign.cjs');`
- Parse subcommand and slug from remaining args (filter --raw)
- Route: init -> cmdCampaignInit(slug, remaining args joined as name, raw), state -> cmdCampaignState(slug, raw), update -> cmdCampaignUpdate(slug, field, value, raw)
- If unknown subcommand: error('campaign subcommand required: init, state, update')
- Update the default error message to include 'campaign' in the available commands list
- Update the JSDoc comment at top of file to include the campaign subcommand
  </action>
  <verify>
    <automated>cd /Users/rishikeshranjan/code/rishiPersonal/takeToMarket && node bin/ttm-tools.cjs campaign init test-campaign "Test Campaign" --raw && ls .marketing/CAMPAIGNS/test-campaign/STATE.md .marketing/CAMPAIGNS/test-campaign/RESEARCH.md .marketing/CAMPAIGNS/test-campaign/BRIEF.md .marketing/CAMPAIGNS/test-campaign/ASSETS/ && node bin/ttm-tools.cjs campaign state test-campaign --raw && node bin/ttm-tools.cjs campaign update test-campaign phase researched --raw && node bin/ttm-tools.cjs campaign state test-campaign --raw | grep researched && rm -rf .marketing/CAMPAIGNS/test-campaign</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "cmdCampaignInit" bin/lib/campaign.cjs
    - grep -q "cmdCampaignState" bin/lib/campaign.cjs
    - grep -q "cmdCampaignUpdate" bin/lib/campaign.cjs
    - grep -q "resolveCampaignStatePath" bin/lib/campaign.cjs
    - grep -q "case 'campaign'" bin/ttm-tools.cjs
    - grep -q "campaign" bin/ttm-tools.cjs (in available commands list)
    - grep -q "phase\\.created" bin/lib/campaign.cjs (flat dot-notation)
    - grep -q "gate\\.positioning_check" bin/lib/campaign.cjs
    - grep -q "startsWith(projectRoot)" bin/lib/campaign.cjs (path security)
    - node bin/ttm-tools.cjs campaign init test-verify "Test" --raw exits 0
  </acceptance_criteria>
  <done>campaign.cjs exports init/state/update functions. ttm-tools.cjs routes campaign subcommand. campaign init creates directory with STATE.md (flat frontmatter), RESEARCH.md (from template), BRIEF.md (from template), and ASSETS/. campaign state reads frontmatter as JSON. campaign update atomically modifies single field.</done>
</task>

<task type="auto">
  <name>Task 2: Create campaign-state.md and campaign-research.md templates</name>
  <files>templates/campaign-state.md, templates/campaign-research.md</files>
  <read_first>
    templates/reference-files/state.md
    templates/campaign-brief.md
    .planning/phases/03-campaign-creation-and-briefing/03-RESEARCH.md
    .planning/phases/03-campaign-creation-and-briefing/03-PATTERNS.md
  </read_first>
  <action>
Create `templates/campaign-state.md` with the flat dot-notation frontmatter schema. This template is used by campaign.cjs cmdCampaignInit to populate new campaign STATE.md files:

```markdown
---
campaign: [SLUG]
name: [CAMPAIGN_NAME]
created: [TIMESTAMP]
phase: created
last_updated: [TIMESTAMP]
phase.created: [TIMESTAMP]
phase.researched: null
phase.briefed: null
phase.produced: null
phase.verified: null
phase.reviewed: null
phase.fixed: null
phase.shipped: null
phase.measured: null
phase.learned: null
gate.positioning_check: null
gate.outcome_metric: null
---

# Campaign: [CAMPAIGN_NAME]

Phase: created
Next step: Run `/ttm-research [SLUG]` to gather market intelligence.
```

Create `templates/campaign-research.md` with structured sections per D-04 (confidence scores, fixed sections). This template is both copied into new campaigns AND referenced by the research workflow:

```markdown
# Research: [CAMPAIGN_NAME]

**Campaign:** [SLUG]
**Researched:** pending
**Method:** pending

## Market Context

<!-- HIGH = verified source URL or cited data; MEDIUM = user-provided paste; LOW = AI inference -->

| Insight | Confidence | Source |
|---------|-----------|--------|
| [GENERATED BY /ttm-research] | HIGH/MEDIUM/LOW | [source or "user-provided"] |

## Competitor Content Analysis

| Competitor | Content Type | Key Message | Gap/Opportunity |
|-----------|-------------|-------------|-----------------|
| [GENERATED BY /ttm-research] | [type] | [message] | [gap] |

## Audience Insights

| Insight | Confidence | Source |
|---------|-----------|--------|
| [GENERATED BY /ttm-research] | HIGH/MEDIUM/LOW | [source] |

## Ambient Narrative

[GENERATED BY /ttm-research -- What the market already believes about this topic]

## Content Gaps

| Gap | Opportunity Size | Difficulty |
|-----|-----------------|------------|
| [GENERATED BY /ttm-research] | HIGH/MEDIUM/LOW | HIGH/MEDIUM/LOW |

## Research Summary

[GENERATED BY /ttm-research -- 2-3 sentence summary for /ttm-brief to consume quickly]
```
  </action>
  <verify>
    <automated>cd /Users/rishikeshranjan/code/rishiPersonal/takeToMarket && grep -q "phase.created" templates/campaign-state.md && grep -q "gate.positioning_check" templates/campaign-state.md && grep -q "gate.outcome_metric" templates/campaign-state.md && grep -q "## Market Context" templates/campaign-research.md && grep -q "## Competitor Content Analysis" templates/campaign-research.md && grep -q "## Audience Insights" templates/campaign-research.md && grep -q "## Ambient Narrative" templates/campaign-research.md && grep -q "## Content Gaps" templates/campaign-research.md && grep -q "## Research Summary" templates/campaign-research.md && grep -q "HIGH/MEDIUM/LOW" templates/campaign-research.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "phase\\.created" templates/campaign-state.md (flat dot-notation)
    - grep -q "phase\\.researched" templates/campaign-state.md
    - grep -q "gate\\.positioning_check" templates/campaign-state.md
    - grep -q "gate\\.outcome_metric" templates/campaign-state.md
    - grep -q "\\[SLUG\\]" templates/campaign-state.md (placeholder for replacement)
    - grep -q "\\[CAMPAIGN_NAME\\]" templates/campaign-state.md
    - grep -q "## Market Context" templates/campaign-research.md
    - grep -q "## Competitor Content Analysis" templates/campaign-research.md
    - grep -q "## Audience Insights" templates/campaign-research.md
    - grep -q "## Ambient Narrative" templates/campaign-research.md
    - grep -q "## Content Gaps" templates/campaign-research.md
    - grep -q "HIGH/MEDIUM/LOW" templates/campaign-research.md (confidence tags per D-04)
  </acceptance_criteria>
  <done>campaign-state.md template has flat dot-notation frontmatter with all 10 phase tracking fields and 2 gate fields. campaign-research.md template has all 5 fixed sections per D-04 with confidence score columns and placeholder markers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input -> slug | Campaign name from user argument passed to filesystem operations |
| Campaign state files | Frontmatter parsed and re-serialized -- injection via field values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | bin/lib/campaign.cjs resolveCampaignStatePath | mitigate | Re-sanitize slug via `/[^a-z0-9-]/g` regex AND path.resolve() + startsWith(projectRoot) check |
| T-03-02 | Tampering | bin/lib/campaign.cjs cmdCampaignInit | mitigate | Validate campaignDir startsWith projectRoot before fs.mkdirSync |
| T-03-03 | Information Disclosure | Campaign state files | accept | Local filesystem only, single-user, no PII -- low-value target |
</threat_model>

<verification>
- `node bin/ttm-tools.cjs campaign init test-verify "Test" --raw` creates scaffold
- `node bin/ttm-tools.cjs campaign state test-verify --raw` returns JSON with phase=created
- `node bin/ttm-tools.cjs campaign update test-verify phase researched --raw` updates field
- Templates contain all required sections and placeholders
</verification>

<success_criteria>
- Campaign CLI creates complete scaffold (STATE.md + RESEARCH.md + BRIEF.md + ASSETS/)
- All frontmatter is flat dot-notation (parseFrontmatter compatible)
- Path traversal is blocked by slug sanitization + path.resolve check
- Templates match RESEARCH.md and PATTERNS.md specifications exactly
</success_criteria>

<output>
After completion, create `.planning/phases/03-campaign-creation-and-briefing/03-01-SUMMARY.md`
</output>
