---
phase: 01-plugin-scaffold-and-tooling
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/reference-files/positioning.md
  - templates/reference-files/brand.md
  - templates/reference-files/icp.md
  - templates/reference-files/channels.md
  - templates/reference-files/state.md
  - templates/reference-files/calendar.md
  - templates/reference-files/competitors.md
  - templates/reference-files/metrics.md
  - templates/reference-files/learnings.md
  - templates/campaign-brief.md
  - templates/claude-md.md
  - templates/agents-md.md
  - workflows/lifecycle/.gitkeep
  - workflows/setup/.gitkeep
  - workflows/utility/.gitkeep
  - workflows/reference-mgmt/.gitkeep
  - workflows/discipline/.gitkeep
  - playbooks/.gitkeep
  - gates/base-gates.md
  - gates/meta-gates.md
  - gates/discipline/.gitkeep
  - references/context-loading.md
autonomous: true
requirements:
  - FOUND-04
  - FOUND-06
  - FOUND-07

must_haves:
  truths:
    - "Templates for all 9 .marketing/ reference files exist with two-tier structure (Tier 1 summary + Tier 2 full content)"
    - "CLAUDE.md and AGENTS.md templates exist with identical core rules for positioning-as-invariant enforcement"
    - "Workflow directory structure has all 5 category subdirectories"
    - "Context loading strategy is documented with explicit Tier 1 budget (<2000 tokens)"
    - ".marketing/ directory template includes CAMPAIGNS/ subdirectory but NOT PLAYBOOKS/"
  artifacts:
    - path: "templates/reference-files/positioning.md"
      provides: "Template for .marketing/POSITIONING.md with _SUMMARY section"
      contains: "_SUMMARY"
    - path: "templates/claude-md.md"
      provides: "Template for generated CLAUDE.md"
      contains: "positioning-as-invariant"
    - path: "templates/agents-md.md"
      provides: "Template for generated AGENTS.md"
      contains: "positioning-as-invariant"
    - path: "references/context-loading.md"
      provides: "Two-tier context loading strategy documentation"
      contains: "Tier 1"
    - path: "gates/base-gates.md"
      provides: "Placeholder for 10 base quality gate definitions"
      contains: "quality gate"
  key_links:
    - from: "templates/reference-files/positioning.md"
      to: ".marketing/POSITIONING.md"
      via: "/ttm-init copies and fills this template"
      pattern: "_SUMMARY"
    - from: "templates/claude-md.md"
      to: "CLAUDE.md (in user project root)"
      via: "/ttm-init generates CLAUDE.md from this template"
      pattern: "positioning-as-invariant"
    - from: "references/context-loading.md"
      to: "workflows/**/*.md"
      via: "Workflows reference this doc for loading strategy"
      pattern: "Tier 1|Tier 2"
---

<objective>
Create all template files, workflow directory scaffolding, gate placeholders, and the context loading strategy documentation.

Purpose: Establishes the template layer and directory organization that all future phases build on. Reference file templates define the two-tier context loading structure. Dual-runtime templates ensure Claude Code and Codex compatibility.
Output: 9 reference file templates, 2 runtime instruction templates, 1 campaign brief template, workflow/playbook/gate directory structure, context loading documentation
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-plugin-scaffold-and-tooling/01-CONTEXT.md
@.planning/phases/01-plugin-scaffold-and-tooling/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create reference file templates with two-tier context loading</name>
  <files>templates/reference-files/positioning.md, templates/reference-files/brand.md, templates/reference-files/icp.md, templates/reference-files/channels.md, templates/reference-files/state.md, templates/reference-files/calendar.md, templates/reference-files/competitors.md, templates/reference-files/metrics.md, templates/reference-files/learnings.md, references/context-loading.md</files>
  <read_first>
    .planning/phases/01-plugin-scaffold-and-tooling/01-CONTEXT.md
    .planning/phases/01-plugin-scaffold-and-tooling/01-RESEARCH.md
  </read_first>
  <action>
Create 9 reference file templates and 1 context loading strategy document (per D-10, D-11, D-20).

**Two-Tier Structure Rule (D-10):** Every reference file template MUST have:
1. A `<!-- _SUMMARY -->` block at the top containing Tier 1 content (<200 words per file)
2. A `<!-- END_SUMMARY -->` delimiter
3. Full Tier 2 content below the delimiter
4. Placeholder sections marked with `[GENERATED BY /ttm-init]` that the onboarding interview fills in

**D-11 compliance:** Total Tier 1 budget across all 9 files must be under ~2,000 tokens. Each file's _SUMMARY should be 80-150 words max.

Create each template file at `templates/reference-files/<name>.md`:

**1. `positioning.md`** -- Template for .marketing/POSITIONING.md:
```markdown
<!-- _SUMMARY: Tier 1 context (loaded universally, <200 words) -->
## Summary
**Category:** [GENERATED BY /ttm-init]
**Target audience:** [GENERATED BY /ttm-init]
**Primary differentiator:** [GENERATED BY /ttm-init]
**Proof points:**
- [GENERATED BY /ttm-init]
- [GENERATED BY /ttm-init]
- [GENERATED BY /ttm-init]
**Must-not-say:** [GENERATED BY /ttm-init]
<!-- END_SUMMARY -->

## Detailed Positioning

### Category Definition
[GENERATED BY /ttm-init -- Full category analysis with market context]

### Competitive Frame
[GENERATED BY /ttm-init -- How we position against alternatives]

### Proof Point Library
[GENERATED BY /ttm-init -- Complete proof points with sources and expiry dates]

### Must-Not-Say Terms
[GENERATED BY /ttm-init -- Banned terms with reasoning for each]

### Positioning History
| Date | Change | Reasoning |
|------|--------|-----------|
| [init date] | Initial positioning | Created via /ttm-init |
```

**2. `brand.md`** -- Template for .marketing/BRAND.md:
_SUMMARY section: Voice archetype (1 line), Tone range (1 line), Key banned words (comma list).
Tier 2 sections: Voice Archetype (detailed), Tone per Context (table: context/tone/example), Banned Words (with reasoning), Proof Points (with sources), Brand Examples (good/bad examples).

**3. `icp.md`** -- Template for .marketing/ICP.md:
_SUMMARY section: Primary segment (1 line), JTBD (1 line), Top 3 pains (bullet list), Anti-ICP (1 line).
Tier 2 sections: Primary Segment (demographics, psychographics), JTBD (jobs-to-be-done framework), Pains and Triggers, Anti-ICP Profile, Customer Language Library (exact phrases customers use).

**4. `channels.md`** -- Template for .marketing/CHANNELS.md:
_SUMMARY section: Active channels (comma list), Primary channel (1 line), Budget split (brief).
Tier 2 sections: Active Channels (table: channel/status/baseline/cadence), Dormant Channels (with reasoning), Banned Channels (with reasoning), Budget Allocation, Channel-Specific Rules.

**5. `state.md`** -- Template for .marketing/STATE.md:
YAML frontmatter with fields: `status: initialized`, `current_phase: none`, `current_campaign: none`, `last_updated: [GENERATED]`, `campaigns: []`.
Body sections: Active Campaigns, Decisions in Flight, Blockers, Experiments, Recent Lessons.

**6. `calendar.md`** -- Template for .marketing/CALENDAR.md:
_SUMMARY section: Current quarter theme (1 line), Next launch date (1 line), Cadence (1 line).
Tier 2 sections: Quarterly Themes (table), Launch Calendar, Always-On Cadence, Blackout Dates.

**7. `competitors.md`** -- Template for .marketing/COMPETITORS.md:
_SUMMARY section: Top 3 competitors (names only), Our positioning vs them (1 sentence).
Tier 2 sections: Direct Competitors (table: name/positioning/strength/weakness), Positioning Map, Share of Voice Baseline, Competitor Content Analysis.

**8. `metrics.md`** -- Template for .marketing/METRICS.md:
_SUMMARY section: Primary outcome metric (1 line), Secondary metrics (comma list), Attribution model (1 line).
Tier 2 sections: Primary Outcome Metric (target, measurement window, data source), Secondary Metrics, Leading Indicators, Baselines, Attribution Model Configuration.

**9. `learnings.md`** -- Template for .marketing/LEARNINGS.md:
_SUMMARY section: Total lessons (count), Last lesson date, Top pattern (1 line).
Tier 2 sections: Root-Cause Taxonomy (categories: positioning drift, weak hook, wrong channel, bad timing, unverifiable claim, broken funnel, creative fatigue), Lessons Log (table: date/campaign/category/lesson/action), Pattern Extraction (winning hooks, angles, formats).

**10. `references/context-loading.md`** -- Context Loading Strategy document:
Must document:
- Tier 1: What files are loaded, when, budget per file, total budget (~2,000 tokens)
- Tier 2: What files are loaded, which workflows trigger full loads
- Table mapping each workflow to which reference files it loads at which tier
- Rules for workflow authors: "Always load Tier 1 summaries. Only load Tier 2 when your workflow needs the full content."
- Implementation: _SUMMARY / END_SUMMARY delimiters in each reference file. AI reads only the _SUMMARY block unless workflow instructs full load.
- Per-file token budgets:

| File | Tier 1 Budget | Loaded By (Tier 2) |
|------|---------------|---------------------|
| POSITIONING.md | ~150 words | produce, verify, positioning-check |
| BRAND.md | ~100 words | produce, verify, brand-refresh |
| ICP.md | ~100 words | produce, verify, icp-refresh, brief |
| CHANNELS.md | ~80 words | brief, measure |
| STATE.md | frontmatter only | state, resume, next, health |
| CALENDAR.md | ~80 words | brief, new-campaign |
| COMPETITORS.md | ~80 words | research, competitor-scan |
| METRICS.md | ~80 words | measure, brief |
| LEARNINGS.md | ~80 words | brief, learn |

IMPORTANT per D-20: The `.marketing/` directory does NOT contain a PLAYBOOKS/ subdirectory. Playbooks live in the plugin's `playbooks/` directory. The template shows only `CAMPAIGNS/` as a subdirectory.
  </action>
  <verify>
    <automated>count=$(ls templates/reference-files/*.md 2>/dev/null | wc -l | tr -d ' ') && echo "Template count: $count" && test "$count" -eq 9 && echo "PASS: 9 templates" && grep -l "_SUMMARY" templates/reference-files/positioning.md templates/reference-files/brand.md templates/reference-files/icp.md && echo "PASS: _SUMMARY found in key templates" && grep -l "END_SUMMARY" templates/reference-files/positioning.md && echo "PASS: END_SUMMARY delimiter present" && test -f references/context-loading.md && grep -q "Tier 1" references/context-loading.md && echo "PASS: context loading doc exists"</automated>
  </verify>
  <acceptance_criteria>
    - 9 files exist in `templates/reference-files/`: positioning.md, brand.md, icp.md, channels.md, state.md, calendar.md, competitors.md, metrics.md, learnings.md
    - `templates/reference-files/positioning.md` contains `<!-- _SUMMARY` and `<!-- END_SUMMARY -->`
    - `templates/reference-files/brand.md` contains `<!-- _SUMMARY`
    - `templates/reference-files/icp.md` contains `<!-- _SUMMARY`
    - All templates with _SUMMARY sections have content between _SUMMARY and END_SUMMARY delimiters that is under 200 words
    - `templates/reference-files/state.md` contains YAML frontmatter with `status:` field
    - `templates/reference-files/learnings.md` contains `Root-Cause Taxonomy`
    - `references/context-loading.md` contains `Tier 1` and `Tier 2` and a table mapping workflows to reference files
    - `references/context-loading.md` contains `~2,000 tokens` or `2000 token` budget reference
    - No template file mentions `PLAYBOOKS/` as a subdirectory of `.marketing/`
    - Each template contains `[GENERATED BY /ttm-init]` placeholders
  </acceptance_criteria>
  <done>All 9 reference file templates exist with two-tier _SUMMARY/END_SUMMARY structure. Context loading strategy is documented with per-file token budgets. Templates are ready for /ttm-init to fill in during Phase 2.</done>
</task>

<task type="auto">
  <name>Task 2: Create dual-runtime templates, campaign brief template, and directory scaffolding</name>
  <files>templates/claude-md.md, templates/agents-md.md, templates/campaign-brief.md, workflows/lifecycle/.gitkeep, workflows/setup/.gitkeep, workflows/utility/.gitkeep, workflows/reference-mgmt/.gitkeep, workflows/discipline/.gitkeep, playbooks/.gitkeep, gates/base-gates.md, gates/meta-gates.md, gates/discipline/.gitkeep</files>
  <read_first>
    .planning/phases/01-plugin-scaffold-and-tooling/01-CONTEXT.md
    .planning/phases/01-plugin-scaffold-and-tooling/01-RESEARCH.md
    templates/reference-files/positioning.md
  </read_first>
  <action>
Create dual-runtime templates, campaign brief template, and all directory scaffolding (per D-06, D-08, D-18, D-19).

**1. `templates/claude-md.md`** -- Template for CLAUDE.md generated by /ttm-init (per D-18, D-19):

This is a TEMPLATE that /ttm-init will copy into the user's project root and fill in. It must contain:

```markdown
# takeToMarket - Project Instructions

## Core Invariant

Every marketing asset ships with a verifiable outcome metric and passes through a positioning-invariant quality gate wall. No asset ships without both, ever.

## Positioning as Invariant

POSITIONING.md is the source of truth for all marketing content. It is:
- **Read-only during campaign execution** -- cannot be edited from within a campaign
- **Loaded into every phase context** -- compact summary in non-produce phases, full document in produce/verify
- **Enforced via quality gates** -- positioning drift gate is Tier 1 (blocking)

To change positioning, use `/ttm-positioning-shift` which requires:
1. Explicit reasoning for the shift
2. Migration plan for existing assets
3. Deprecation schedule
4. Human approval

## Outcome Over Output

Every campaign brief must define:
- **Outcome metric**: What business result we expect (e.g., "20% increase in trial starts")
- **Output metric**: What we produce (e.g., "4 blog posts, 2 emails")

Outcome is reported first. Output without outcome is not a campaign.

## Campaign Lifecycle

Campaigns follow a 9-phase lifecycle: Discover -> Brief -> Produce -> Verify -> Review -> Fix -> Ship -> Measure -> Learn

Each phase has a dedicated `/ttm-*` command. Phases cannot be skipped.

## File Paths

- Marketing state: `.marketing/`
- Reference files: `.marketing/POSITIONING.md`, `.marketing/BRAND.md`, etc.
- Campaigns: `.marketing/CAMPAIGNS/<slug>/`
- Campaign state: `.marketing/CAMPAIGNS/<slug>/STATE.md`

## Deterministic Operations

Always use `ttm-tools.cjs` for:
- Slug generation: `node ttm-tools.cjs slug "campaign name"`
- Timestamps: `node ttm-tools.cjs timestamp`
- State updates: `node ttm-tools.cjs state update <field> <value>`
- Health checks: `node ttm-tools.cjs health`

Never generate slugs or timestamps via AI -- they must be deterministic.

## Quality Gate Wall

Assets pass through 10 base quality gates:
1. Positioning drift (Tier 1 - blocking)
2. Claim accuracy (Tier 1 - blocking)
3. Voice drift
4. Outcome alignment (Tier 1 - blocking)
5. Funnel integrity
6. UTM hygiene
7. Compliance
8. Competitor collision
9. ICP fit
10. Format correctness

Tier 1 gates are blocking. Tier 2 gates are advisory.
```

**2. `templates/agents-md.md`** -- Template for AGENTS.md (Codex) (per D-18, D-19):
MUST contain identical core rules as claude-md.md (per D-19). Only differences:
- Title: "takeToMarket - Codex Instructions"
- Remove any Claude Code-specific references
- Same sections: Core Invariant, Positioning as Invariant, Outcome Over Output, Campaign Lifecycle, File Paths, Deterministic Operations, Quality Gate Wall
- The content is functionally identical to claude-md.md

**3. `templates/campaign-brief.md`** -- Template for campaign brief output:
Must contain sections: Campaign Name, Goal, Outcome Metric (with target value and measurement window), Output Metric, ICP Segment, Positioning Anchor, Hook, Proof Points, Channel Mix, Assets List, Success Criteria, Failure Criteria, Dependencies, Timeline.

**4. Workflow directory scaffolding** (per D-06, D-08):
Create empty `.gitkeep` files in each workflow category directory:
- `workflows/lifecycle/.gitkeep`
- `workflows/setup/.gitkeep`
- `workflows/utility/.gitkeep`
- `workflows/reference-mgmt/.gitkeep`
- `workflows/discipline/.gitkeep`

Per D-08: Do NOT create `contexts/` or `agents/` directories. Those come in Phase 4.

**5. Other directory scaffolding:**
- `playbooks/.gitkeep` (playbook content comes in Phase 8)
- `gates/discipline/.gitkeep` (discipline-specific gates come in Phase 8)

**6. Gate placeholder files:**
- `gates/base-gates.md` -- Placeholder listing all 10 base quality gates by name and tier. Content format:
  ```markdown
  # Base Quality Gates
  
  **Status:** Gate definitions will be implemented in Phase 4
  
  ## Tier 1 (Blocking)
  1. **Positioning Drift** -- GATE-01
  2. **Claim Accuracy** -- GATE-02
  3. **Outcome Alignment** -- GATE-04
  
  ## Tier 2 (Advisory)
  4. **Voice Drift** -- GATE-03
  5. **Funnel Integrity** -- GATE-05
  6. **UTM Hygiene** -- GATE-06
  7. **Compliance** -- GATE-07
  8. **Competitor Collision** -- GATE-08
  9. **ICP Fit** -- GATE-09
  10. **Format Correctness** -- GATE-10
  ```

- `gates/meta-gates.md` -- Placeholder listing 4 meta-gates by name. Content: Portfolio Balance (META-01), Calendar Collision (META-02), Theme Consistency (META-03), Learning Plan (META-04). Status: Phase 9.
  </action>
  <verify>
    <automated>test -f templates/claude-md.md && test -f templates/agents-md.md && test -f templates/campaign-brief.md && echo "PASS: runtime templates exist" && test -d workflows/lifecycle && test -d workflows/setup && test -d workflows/utility && test -d workflows/reference-mgmt && test -d workflows/discipline && echo "PASS: workflow dirs exist" && test -d playbooks && test -d gates/discipline && echo "PASS: playbook/gate dirs exist" && grep -q "positioning-as-invariant" templates/claude-md.md && grep -q "positioning-as-invariant" templates/agents-md.md && echo "PASS: both runtime templates have positioning invariant" && test -f gates/base-gates.md && grep -q "GATE-01" gates/base-gates.md && echo "PASS: base gates placeholder exists" && test ! -d contexts && test ! -d agents && echo "PASS: no contexts/ or agents/ directory (per D-08)"</automated>
  </verify>
  <acceptance_criteria>
    - `templates/claude-md.md` contains `positioning-as-invariant`, `Outcome Over Output`, `Campaign Lifecycle`, `Quality Gate Wall`
    - `templates/agents-md.md` contains `positioning-as-invariant`, `Outcome Over Output`, `Campaign Lifecycle`, `Quality Gate Wall`
    - Both claude-md.md and agents-md.md contain the same core rules (Outcome Over Output, 9-phase lifecycle, deterministic operations, quality gate wall)
    - `templates/campaign-brief.md` contains `Outcome Metric` and `Output Metric` sections
    - Directories exist: `workflows/lifecycle/`, `workflows/setup/`, `workflows/utility/`, `workflows/reference-mgmt/`, `workflows/discipline/`
    - Directories exist: `playbooks/`, `gates/discipline/`
    - `gates/base-gates.md` contains `GATE-01` through `GATE-10` and separates Tier 1 from Tier 2
    - `gates/meta-gates.md` contains `META-01` through `META-04`
    - No `contexts/` or `agents/` directory exists at plugin root (per D-08)
    - `templates/claude-md.md` contains `node ttm-tools.cjs slug` reference
  </acceptance_criteria>
  <done>All templates, workflow directories, and gate placeholders exist. Dual-runtime templates have identical core rules. Campaign brief template enforces outcome metrics. Directory structure matches D-04 and D-06 exactly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Template content -> user project files | Templates are copied and filled by /ttm-init into user's project |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | templates/claude-md.md | accept | Template is static Markdown. If tampered in the plugin, /ttm-init would copy tampered content -- but plugin integrity is the user's responsibility (they install via git clone or npm). Standard open-source trust model. |
| T-03-02 | Information Disclosure | templates/reference-files/*.md | accept | Templates contain placeholder structure only. No actual business data until filled by /ttm-init. |
</threat_model>

<verification>
- `ls templates/reference-files/*.md | wc -l` returns 9
- `ls templates/claude-md.md templates/agents-md.md templates/campaign-brief.md` all exist
- `ls -d workflows/lifecycle workflows/setup workflows/utility workflows/reference-mgmt workflows/discipline` all exist
- `test -f gates/base-gates.md && test -f gates/meta-gates.md` both pass
- `grep -c "_SUMMARY" templates/reference-files/positioning.md` returns at least 1
- `diff <(grep "positioning-as-invariant" templates/claude-md.md) <(grep "positioning-as-invariant" templates/agents-md.md)` shows both contain the term
</verification>

<success_criteria>
All 9 reference file templates exist with two-tier _SUMMARY structure and <200 word Tier 1 summaries. CLAUDE.md and AGENTS.md templates contain identical core rules. Context loading strategy is documented. Workflow directory structure has all 5 categories. Gate placeholders list all 10 base gates and 4 meta-gates. No contexts/ or agents/ directories exist (per D-08).
</success_criteria>

<output>
After completion, create `.planning/phases/01-plugin-scaffold-and-tooling/01-03-SUMMARY.md`
</output>
