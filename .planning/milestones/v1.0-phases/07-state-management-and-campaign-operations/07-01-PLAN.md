---
phase: 07-state-management-and-campaign-operations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/lib/campaign.cjs
  - bin/lib/health.cjs
  - bin/ttm-tools.cjs
  - references/learnings-extraction.md
  - templates/reference-files/learnings.md
autonomous: true
requirements: [STAT-03, STAT-04, STAT-05]

must_haves:
  truths:
    - "campaign archive subcommand moves directory from CAMPAIGNS/<slug>/ to CAMPAIGNS/ARCHIVE/<slug>/ and updates state to archived"
    - "health --full subcommand runs extended checks (staleness, velocity, state consistency, DRIFT-LOG integrity)"
    - "ALLOWED_FIELDS includes Phase 7 archive and cancel tracking fields"
    - "learnings-extraction.md reference file provides structured extraction guide with root-cause taxonomy"
  artifacts:
    - path: "bin/lib/campaign.cjs"
      provides: "cmdCampaignArchive function"
      exports: ["cmdCampaignInit", "cmdCampaignState", "cmdCampaignUpdate", "cmdCampaignList", "cmdCampaignArchive"]
    - path: "bin/lib/health.cjs"
      provides: "Extended cmdHealth with --full flag"
      exports: ["cmdHealth", "cmdInit"]
    - path: "bin/ttm-tools.cjs"
      provides: "Router with campaign archive and health --full subcommands"
    - path: "references/learnings-extraction.md"
      provides: "Structured learnings extraction guide for archive workflow"
    - path: "templates/reference-files/learnings.md"
      provides: "LEARNINGS.md template with append marker"
      contains: "<!-- LESSONS BELOW THIS LINE -->"
  key_links:
    - from: "bin/ttm-tools.cjs"
      to: "bin/lib/campaign.cjs"
      via: "require and dispatch cmdCampaignArchive"
      pattern: "cmdCampaignArchive"
    - from: "bin/ttm-tools.cjs"
      to: "bin/lib/health.cjs"
      via: "cmdHealth(raw, full) with --full flag"
      pattern: "cmdHealth.*full"
---

<objective>
Extend the CLI infrastructure to support Phase 7 operations: campaign archival (directory move + state update), extended health auditing (staleness, velocity, state consistency, DRIFT-LOG integrity), and learnings extraction reference content.

Purpose: All five Phase 7 workflows depend on these CLI primitives. Building them first ensures workflows can call deterministic operations via `bin/ttm-tools.cjs` without hand-rolling filesystem or validation logic.

Output: Extended campaign.cjs with cmdCampaignArchive, extended health.cjs with full audit mode, updated ttm-tools.cjs router, learnings-extraction.md reference file, and LEARNINGS.md template with append marker.
</objective>

<execution_context>
@/Users/rishikeshranjan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rishikeshranjan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-state-management-and-campaign-operations/07-RESEARCH.md
@.planning/phases/07-state-management-and-campaign-operations/07-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From bin/lib/core.cjs:
```javascript
function output(result, raw, rawValue) { /* JSON or raw string to stdout */ }
function error(message) { /* stderr + process.exit(1) */ }
function safeReadFile(filePath) { /* returns string or null */ }
function safeWriteFile(filePath, content) { /* atomic write */ }
function parseFrontmatter(content) { /* returns { frontmatter, body } */ }
function serializeFrontmatter(frontmatter, body) { /* returns string */ }
```

From bin/lib/campaign.cjs:
```javascript
function resolveCampaignStatePath(slug) { /* sanitizes slug, resolves path, security check */ }
const ALLOWED_FIELDS = new Set([ 'phase', 'name', 'last_updated', 'phase.created', ... ]);
const ACTIVE_PHASES = new Set(['briefed', 'produced', 'verified', 'reviewed', 'shipped']);
module.exports = { cmdCampaignInit, cmdCampaignState, cmdCampaignUpdate, cmdCampaignList };
```

From bin/lib/health.cjs:
```javascript
function dirExists(p) { /* fs.statSync check */ }
function fileExists(p) { /* fs.statSync check */ }
const REFERENCE_FILES = [ 'POSITIONING.md', 'BRAND.md', 'ICP.md', ... ];
function cmdHealth(raw) { /* returns checks array with {name, status, path?, detail?} */ }
module.exports = { cmdHealth, cmdInit };
```

From bin/ttm-tools.cjs (campaign case, lines 65-85):
```javascript
case 'campaign': {
  const { cmdCampaignInit, cmdCampaignState, cmdCampaignUpdate, cmdCampaignList } = require('./lib/campaign.cjs');
  // ... subcommand dispatch: init, state, update, list
}
case 'health': {
  const { cmdHealth } = require('./lib/health.cjs');
  cmdHealth(raw);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend campaign.cjs with cmdCampaignArchive and Phase 7 ALLOWED_FIELDS</name>
  <files>bin/lib/campaign.cjs</files>
  <read_first>bin/lib/campaign.cjs, bin/lib/core.cjs, templates/campaign-state.md</read_first>
  <action>
Add Phase 7 fields to ALLOWED_FIELDS Set (per D-07, D-08):
- `'archive.archived_at'`
- `'archive.learnings_extracted'`
- `'cancel.cancelled_at'`
- `'cancel.reason'`

Add `'cancelled'` handling: campaigns with phase='cancelled' should be recognized but NOT archivable (per D-08).

Create `cmdCampaignArchive(slug, raw)` function following the exact pattern of cmdCampaignInit:
1. Sanitize slug: `const safe = slug.toLowerCase().replace(/[^a-z0-9-]/g, '');`
2. Resolve source path: `path.resolve(process.cwd(), '.marketing', 'CAMPAIGNS', safe)`
3. Resolve destination: `path.resolve(process.cwd(), '.marketing', 'CAMPAIGNS', 'ARCHIVE', safe)`
4. Security check: both paths must `startsWith(projectRoot)`
5. Validate source exists and destination does NOT exist (per D-10 irreversibility)
6. Read source STATE.md, parse frontmatter, validate phase is `'shipped'` (per D-08). If phase is not 'shipped', call `error('Only shipped campaigns can be archived. Current phase: ' + frontmatter.phase)`.
7. Create ARCHIVE/ directory with `fs.mkdirSync(path.dirname(destDir), { recursive: true })`
8. Copy: `fs.cpSync(srcDir, destDir, { recursive: true })` (cross-filesystem safe per Pitfall 1)
9. Verify destination exists before removing source
10. Remove source: `fs.rmSync(srcDir, { recursive: true, force: true })`
11. Update STATE.md in archived location: set `phase` to `'archived'`, set `archive.archived_at` to ISO timestamp, set `last_updated` to same timestamp
12. Output: `output({ archived: true, slug: safe, dest: destDir }, raw, 'archived ' + safe)`

Update module.exports to include cmdCampaignArchive.

Update module header comment to list new export.

Do NOT modify ACTIVE_PHASES -- the /ttm-next workflow will handle filtering in the AI layer (per Pitfall 2 resolution in RESEARCH.md).
  </action>
  <verify>
    <automated>node bin/ttm-tools.cjs campaign archive nonexistent-campaign --raw 2>&1 | grep -c "Error"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "cmdCampaignArchive" bin/lib/campaign.cjs returns >= 2 (definition + export)
    - grep -c "archive.archived_at" bin/lib/campaign.cjs returns >= 2 (ALLOWED_FIELDS + usage)
    - grep -c "archive.learnings_extracted" bin/lib/campaign.cjs returns >= 1
    - grep -c "cancel.cancelled_at" bin/lib/campaign.cjs returns >= 1
    - grep -c "cancel.reason" bin/lib/campaign.cjs returns >= 1
    - grep -c "cpSync" bin/lib/campaign.cjs returns >= 1
    - grep -c "Only shipped campaigns" bin/lib/campaign.cjs returns >= 1
    - node bin/ttm-tools.cjs campaign archive nonexistent-campaign --raw 2>&1 exits with error
  </acceptance_criteria>
  <done>cmdCampaignArchive validates shipped-only, moves directory cross-filesystem safely, updates state to archived with timestamp, and is exported from campaign.cjs</done>
</task>

<task type="auto">
  <name>Task 2: Extend health.cjs with full audit mode and update ttm-tools.cjs router</name>
  <files>bin/lib/health.cjs, bin/ttm-tools.cjs</files>
  <read_first>bin/lib/health.cjs, bin/ttm-tools.cjs, bin/lib/core.cjs, bin/lib/drift-log.cjs</read_first>
  <action>
**health.cjs extension (per D-11, D-12, D-13):**

Modify `cmdHealth` signature to accept a second parameter: `cmdHealth(raw, full)`. When `full` is falsy, run existing basic checks only (backward compatible). When `full` is truthy, run ALL checks including the new categories below.

Add new check functions (each returns an array of check objects with `{name, status, path?, detail?}` shape):

1. `checkCampaignStateConsistency(campaignsDir)` -- for each campaign directory under CAMPAIGNS/ (skip ARCHIVE/):
   - Verify STATE.md exists
   - Parse frontmatter, validate `phase` field is one of: `created`, `researched`, `briefed`, `produced`, `verified`, `reviewed`, `fixed`, `shipped`, `measured`, `learned`, `archived`, `cancelled`
   - Status: 'fail' if missing or invalid phase, 'pass' otherwise

2. `checkReferenceStaleness(marketingDir, thresholdDays)` -- for each reference file in REFERENCE_FILES:
   - Check file mtime via `fs.statSync(filePath).mtimeMs`
   - If older than thresholdDays (default 90), status = 'warn', detail = `not updated in ${days} days`
   - Otherwise status = 'pass'

3. `checkCampaignVelocity(campaignsDir, thresholdDays)` -- for each active campaign:
   - Parse `last_updated` from frontmatter
   - If more than thresholdDays (default 14) since last_updated, status = 'warn', detail = `stuck in ${phase} for ${days} days`

4. `checkDriftLogIntegrity(marketingDir)` -- structural check only (per Open Question 3 -- do NOT re-evaluate drift):
   - File exists
   - Contains the audit trail marker `<!-- Audit Trail -->`
   - No duplicate markers
   - Status: 'pass', 'warn' (no drift log yet), or 'fail' (corrupt)

5. `checkGateConsistency(campaignsDir)` -- for each campaign with verify results:
   - Validate that gate result fields are one of: `null`, `pass`, `warn`, `fail`
   - Status: 'fail' if invalid values found

In the `cmdHealth` function body, after existing checks, if `full` is truthy:
- Call each new check function
- Push all results into the `checks` array
- Add a `'warn'` status level to the summary calculation (alongside pass/fail/missing)

The overall `healthy` flag should be `false` if any check has status `'fail'`. Warnings do not make it unhealthy but are reported.

Output shape unchanged: `{ healthy, checks, summary }` (per D-12 -- text output, no file).

**ttm-tools.cjs router updates:**

1. In the `campaign` case block, add `archive` subcommand:
   ```javascript
   else if (subCmd === 'archive') cmdCampaignArchive(slug, raw);
   ```
   Update the require destructuring to include `cmdCampaignArchive`.
   Update the error message to include `archive` in the list of subcommands.

2. In the `health` case block, pass `--full` flag:
   ```javascript
   const full = args.includes('--full');
   cmdHealth(raw, full);
   ```

3. Update the default error message to include all available commands.
  </action>
  <verify>
    <automated>node bin/ttm-tools.cjs health --raw 2>&1 | grep -c "checks passed" && node bin/ttm-tools.cjs health --full --raw 2>&1 | grep -c "checks passed"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "checkCampaignStateConsistency" bin/lib/health.cjs returns >= 1
    - grep -c "checkReferenceStaleness" bin/lib/health.cjs returns >= 1
    - grep -c "checkCampaignVelocity" bin/lib/health.cjs returns >= 1
    - grep -c "checkDriftLogIntegrity" bin/lib/health.cjs returns >= 1
    - grep -c "checkGateConsistency" bin/lib/health.cjs returns >= 1
    - grep -v '^#' bin/lib/health.cjs | grep -c "function cmdHealth" returns 1 (signature updated)
    - grep -c "cmdCampaignArchive" bin/ttm-tools.cjs returns >= 1
    - grep -c "'archive'" bin/ttm-tools.cjs returns >= 1
    - grep -c "'--full'" bin/ttm-tools.cjs returns >= 1
    - node bin/ttm-tools.cjs health --raw exits 0
    - node bin/ttm-tools.cjs health --full --raw exits 0
  </acceptance_criteria>
  <done>health.cjs runs 5 new check categories when --full flag is passed; ttm-tools.cjs routes campaign archive and health --full subcommands correctly; backward compatibility maintained for basic health check</done>
</task>

<task type="auto">
  <name>Task 3: Create learnings-extraction.md reference file and add LEARNINGS.md append marker</name>
  <files>references/learnings-extraction.md, templates/reference-files/learnings.md</files>
  <read_first>references/ship-checklist-items.md, templates/reference-files/learnings.md</read_first>
  <action>
**references/learnings-extraction.md** -- Create a new reference file following the exact formatting pattern of `references/ship-checklist-items.md` (plain Markdown, no frontmatter, headings and content). This file is loaded via `@`-syntax in the archive workflow to keep the workflow under 500 lines.

Content must include (per D-09):

1. **Extraction Structure** section:
   - "What Worked" subsection: Instructions to scan campaign artifacts for gate passes (especially first-attempt passes), high review scores, assets that shipped without fix loops. Extract the pattern or approach that drove success.
   - "What Didn't Work" subsection: Instructions to scan for gate failures, fix loop data (fix_attempts > 0), review rejections. For each failure, categorize using the root-cause taxonomy.
   - "Campaign-Level Decisions" subsection: Instructions to extract key decisions made during the campaign (positioning anchor chosen, channel mix rationale, hook strategy) for future reference.

2. **Root-Cause Taxonomy** section (per LEARNINGS.md template, same categories):
   - positioning-drift, weak-hook, wrong-channel, bad-timing, unverifiable-claim, broken-funnel, creative-fatigue
   - Each with a one-line description matching the LEARNINGS.md template table

3. **Output Format** section:
   - Each extracted lesson must produce a table row: `| Date | Campaign | Category | Lesson | Action Taken |`
   - Date = archive date (today)
   - Campaign = campaign slug
   - Category = one of the root-cause taxonomy values (or "success" for wins)
   - Lesson = one-sentence summary of what happened and why
   - Action Taken = recommended or completed reference file update, or "none"

4. **Artifact Scanning Guide** section:
   - List the specific files to read from the campaign directory: STATE.md (frontmatter for gate results, fix counts, phase timestamps), MANIFEST.json (per-asset results), BRIEF.md (original strategy), VERIFY-REPORT-*.md (gate details), FIX-BRIEF-*.md (fix attempts), REVIEW-FEEDBACK-*.md (human feedback)

**templates/reference-files/learnings.md** -- Add a marker comment for append-based insertion (following drift-log.cjs pattern per Pitfall 4). Insert `<!-- LESSONS BELOW THIS LINE -->` on a new line immediately AFTER the Lessons Log table header row (`| Date | Campaign | Category | Lesson | Action Taken |` and the separator `|------|----------|----------|--------|-------------|`), and BEFORE the placeholder row `| [populated during Learn phase] | | | | |`. Remove the placeholder row since the marker replaces it as the insertion point.
  </action>
  <verify>
    <automated>test -f references/learnings-extraction.md && grep -c "Root-Cause Taxonomy" references/learnings-extraction.md && grep -c "LESSONS BELOW THIS LINE" templates/reference-files/learnings.md</automated>
  </verify>
  <acceptance_criteria>
    - test -f references/learnings-extraction.md succeeds
    - grep -c "What Worked" references/learnings-extraction.md returns >= 1
    - grep -c "What Didn't Work" references/learnings-extraction.md returns >= 1
    - grep -c "Campaign-Level Decisions" references/learnings-extraction.md returns >= 1
    - grep -c "Root-Cause Taxonomy" references/learnings-extraction.md returns >= 1
    - grep -c "positioning-drift" references/learnings-extraction.md returns >= 1
    - grep -c "Output Format" references/learnings-extraction.md returns >= 1
    - grep -c "Artifact Scanning Guide" references/learnings-extraction.md returns >= 1
    - grep -c "LESSONS BELOW THIS LINE" templates/reference-files/learnings.md returns 1
    - grep -c "populated during Learn phase" templates/reference-files/learnings.md returns 0
  </acceptance_criteria>
  <done>learnings-extraction.md provides complete structured extraction guide with root-cause taxonomy, output format, and artifact scanning instructions; LEARNINGS.md template has append marker for safe row insertion</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input -> CLI slug parameter | Untrusted slug string passed to campaign archive subcommand |
| Filesystem -> CLI state reads | Campaign STATE.md content could be manually corrupted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | cmdCampaignArchive slug parameter | mitigate | Sanitize slug via `slug.toLowerCase().replace(/[^a-z0-9-]/g, '')`, resolve path with `path.resolve`, verify result `startsWith(projectRoot)` -- same pattern as existing cmdCampaignInit |
| T-07-02 | Tampering | Archive destination path escape | mitigate | Destination path must also pass `startsWith(projectRoot)` check before any filesystem operation |
| T-07-03 | Tampering | ALLOWED_FIELDS bypass for new fields | mitigate | Add new fields to ALLOWED_FIELDS Set explicitly -- existing allowlist pattern prevents arbitrary field injection |
| T-07-04 | Denial of Service | Health check on large campaign directory | accept | Health check iterates campaign directories sequentially; bounded by filesystem entries. Unlikely to be problematic for a local skill. |
| T-07-05 | Information Disclosure | Health check reads all campaign STATE.md files | accept | Local filesystem skill -- user has full access to their own .marketing/ directory. No cross-user boundary. |
</threat_model>

<verification>
- `node bin/ttm-tools.cjs campaign archive nonexistent --raw 2>&1` exits with error mentioning "not found"
- `node bin/ttm-tools.cjs health --raw` passes with existing checks
- `node bin/ttm-tools.cjs health --full --raw` passes with extended checks
- `test -f references/learnings-extraction.md` succeeds
- `grep "LESSONS BELOW THIS LINE" templates/reference-files/learnings.md` finds the marker
</verification>

<success_criteria>
- cmdCampaignArchive function exists, validates shipped-only, moves cross-filesystem safely, updates archived state
- cmdHealth accepts --full flag and runs 5 additional check categories
- ttm-tools.cjs routes campaign archive and health --full correctly
- learnings-extraction.md provides structured extraction guide
- LEARNINGS.md template has append marker
- All existing CLI commands continue to work (backward compatibility)
</success_criteria>

<output>
After completion, create `.planning/phases/07-state-management-and-campaign-operations/07-01-SUMMARY.md`
</output>
