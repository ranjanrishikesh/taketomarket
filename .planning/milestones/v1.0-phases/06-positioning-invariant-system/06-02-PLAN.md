---
phase: 06-positioning-invariant-system
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - workflows/reference-mgmt/positioning-check.md
  - references/positioning-check-report.md
  - skills/ttm-positioning-check/SKILL.md
autonomous: true
requirements:
  - POSN-04

must_haves:
  truths:
    - "User runs /ttm-positioning-check and receives a drift audit report covering all assets produced in the configurable time window"
    - "Report shows per-asset drift percentage (WARN+FAIL checks / total checks) and aggregate drift percentage across all sampled assets"
    - "Report shows drift types using GATE-01 categories: differentiator alignment, proof point sourcing, must-not-say violations"
    - "Report includes trend comparison to last audit if a prior audit entry exists in DRIFT-LOG.md"
    - "Report cross-references accepted deviations from campaign DEVIATIONS.md files"
    - "Audit results are appended to DRIFT-LOG.md via CLI"
    - "After every 3rd campaign ships since last audit, the ship workflow suggests running positioning-check"
  artifacts:
    - path: "workflows/reference-mgmt/positioning-check.md"
      provides: "Drift audit workflow with GATE-01 reuse"
      contains: "GATE-01"
    - path: "references/positioning-check-report.md"
      provides: "Report format template extracted to keep workflow under 500 lines"
      contains: "Drift Categories"
    - path: "skills/ttm-positioning-check/SKILL.md"
      provides: "Updated from stub to final with routing"
  key_links:
    - from: "workflows/reference-mgmt/positioning-check.md"
      to: "bin/ttm-tools.cjs campaign list"
      via: "CLI invocation for campaign enumeration"
      pattern: "ttm-tools.cjs.*campaign list"
    - from: "workflows/reference-mgmt/positioning-check.md"
      to: "bin/ttm-tools.cjs drift-log append"
      via: "CLI invocation for audit logging"
      pattern: "ttm-tools.cjs.*drift-log append"
    - from: "workflows/reference-mgmt/positioning-check.md"
      to: "gates/gate-evaluation.md"
      via: "@-reference for GATE-01 evaluation logic"
      pattern: "gate-evaluation"
---

<objective>
Create the /ttm-positioning-check drift audit workflow that samples recent assets across campaigns, evaluates each against GATE-01 positioning drift checks, generates a structured report with per-asset and aggregate drift metrics, and logs the audit to DRIFT-LOG.md.

Purpose: Delivers the cross-campaign positioning drift audit capability (POSN-04) that allows users to monitor positioning adherence over time.

Output: 3 files (2 new workflows/references, 1 updated SKILL.md).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-positioning-invariant-system/06-RESEARCH.md
@.planning/phases/06-positioning-invariant-system/06-PATTERNS.md
@.planning/phases/06-positioning-invariant-system/06-01-SUMMARY.md

<interfaces>
<!-- Existing interfaces the executor needs -->

From gates/gate-evaluation.md (GATE-01 section to reuse):
```markdown
### Evaluating GATE-01: Positioning Drift
**Load:** `.marketing/POSITIONING.md` (Tier 2 full)
**Asset content:** Full asset text
**Evaluate:**
1. Does the asset restate or naturally extend the primary differentiator from POSITIONING.md? Or does it introduce a different claim?
2. Are all factual claims in the asset backed by proof points in the POSITIONING.md proof point library?
3. Does the asset contain any terms from the POSITIONING.md must-not-say list?
```

From bin/ttm-tools.cjs CLI (created in Plan 01):
```bash
# Campaign enumeration
node "${CLAUDE_PLUGIN_ROOT}/bin/ttm-tools.cjs" campaign list --since 30d --raw
# Returns: { "campaigns": [...], "count": N }

# Drift log append
node "${CLAUDE_PLUGIN_ROOT}/bin/ttm-tools.cjs" drift-log append \
  --event-type audit \
  --source "/ttm-positioning-check" \
  --details "[summary]" \
  --affected [count]
```

From references/context-loading.md loading matrix:
```
| /ttm-positioning-check | Yes | POSITIONING.md |
```

From workflows/lifecycle/verify.md (workflow wrapper pattern):
```markdown
<purpose>...</purpose>
<required_reading>@${CLAUDE_PLUGIN_ROOT}/references/context-loading.md</required_reading>
<process>## Text-Mode Detection ... ## Step 1: Load Context ... </process>
<success_criteria>...</success_criteria>
<output>...</output>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create positioning-check-report.md reference and positioning-check.md workflow</name>
  <files>references/positioning-check-report.md, workflows/reference-mgmt/positioning-check.md, skills/ttm-positioning-check/SKILL.md</files>
  <read_first>
    - workflows/lifecycle/verify.md (primary analog -- workflow wrapper, context loading, gate evaluation, completion banner patterns)
    - gates/gate-evaluation.md (GATE-01 section lines 55-67 -- reuse for drift evaluation)
    - references/context-loading.md (loading matrix -- positioning-check loads Tier 2 POSITIONING.md)
    - references/review-checklist.md (reference file structure analog)
    - workflows/lifecycle/brief-positioning-check.md (5-check pattern for drift categorization)
    - skills/ttm-positioning-check/SKILL.md (current stub to update)
    - templates/drift-log.md (created in Plan 01 -- understand DRIFT-LOG.md schema)
    - workflows/lifecycle/ship.md (for auto-suggest insertion point after ship completion)
  </read_first>
  <action>
**Step 1: Create `references/positioning-check-report.md`**

This is a supporting reference file loaded via @-syntax. Structure:

```markdown
# Positioning Check Report Format

## Usage
Referenced by `workflows/reference-mgmt/positioning-check.md` via @-syntax.
Defines the audit report format, drift categorization, and trend comparison logic.

## Drift Categories

Drift is evaluated using GATE-01's 3 checks. Each check maps to a category:

| Check # | Category | PASS Criteria | WARN Criteria | FAIL Criteria |
|---------|----------|---------------|---------------|---------------|
| 1 | Differentiator Alignment | Asset restates or naturally extends primary differentiator | Asset is neutral -- neither reinforces nor contradicts | Asset introduces a different/competing claim |
| 2 | Proof Point Sourcing | All factual claims backed by POSITIONING.md proof points | Some claims present but not all sourced | Claims contradict or fabricate proof points |
| 3 | Must-Not-Say Compliance | No terms from must-not-say list appear | Terms appear in non-customer-facing context | Must-not-say terms in customer-facing copy |

## Per-Asset Drift Calculation

Per-asset drift percentage = (WARN checks + FAIL checks) / total checks evaluated * 100

- 3 checks evaluated per asset
- PASS = 0 drift points, WARN = 1 drift point, FAIL = 1 drift point
- 0% = fully on-positioning, 33% = one check drifted, 67% = two checks drifted, 100% = fully off-positioning

## Aggregate Drift Calculation

Aggregate drift % = total drift points across all assets / (total assets * 3) * 100

## Report Template

Present the audit report in this format:

[Include the full report template with sections:
- Header: "takeToMarket > POSITIONING AUDIT REPORT"
- Audit window and asset count
- Aggregate drift percentage with trend arrow (up/down/stable vs last audit)
- Per-asset results table: Asset | Campaign | Check 1 | Check 2 | Check 3 | Drift %
- Drift type breakdown: count by category
- Accepted deviations cross-reference: list GATE-01 deviations from campaign DEVIATIONS.md files
- Trend comparison: if prior audit exists in DRIFT-LOG.md, show delta
- Recommendations: suggest /ttm-positioning-shift if aggregate drift > 30%, suggest /ttm-fix for specific assets]

## Cross-Reference Logic

For each campaign in the audit window:
1. Read `CAMPAIGNS/<slug>/DEVIATIONS.md` if it exists
2. Filter for rows where Gate = `positioning_drift`
3. Include in the "Accepted Deviations" section of the report
4. Count these as part of the aggregate drift calculation
```

**Step 2: Create `workflows/reference-mgmt/` directory and `positioning-check.md`**

Create directory `workflows/reference-mgmt/` if it doesn't exist.

Create `workflows/reference-mgmt/positioning-check.md` following verify.md wrapper pattern:

```markdown
<purpose>
Positioning drift audit workflow for /ttm-positioning-check. Samples recent assets
across all campaigns within a configurable time window (default 30 days per D-01),
evaluates each against GATE-01 positioning drift checks, generates a structured
audit report, and appends the results to .marketing/DRIFT-LOG.md.
</purpose>

<required_reading>
@${CLAUDE_PLUGIN_ROOT}/references/context-loading.md
@${CLAUDE_PLUGIN_ROOT}/gates/gate-evaluation.md
@${CLAUDE_PLUGIN_ROOT}/references/positioning-check-report.md
</required_reading>

<process>

## Text-Mode Detection
[Same pattern as verify.md lines 17-38]

## Step 1: Load Context
[Load Tier 1 summaries from all 9 .marketing/ reference files per context-loading.md]
[Load Tier 2: .marketing/POSITIONING.md (full)]

## Step 2: Parse Time Window
Parse optional `--since` argument from $ARGUMENTS. Default: 30d (per D-01).
Extract number and unit (d=days). Calculate cutoff date.

## Step 3: Enumerate Campaigns and Assets
Run campaign enumeration:
```bash
CAMPAIGNS_JSON=$(node "${CLAUDE_PLUGIN_ROOT}/bin/ttm-tools.cjs" campaign list --since ${WINDOW} --raw)
```

If count is 0: display "No campaigns with assets in the last ${WINDOW}. Nothing to audit." and exit.

For each campaign in the result:
- Read the campaign's ASSETS/ directory (via Glob: `.marketing/CAMPAIGNS/${SLUG}/ASSETS/*`)
- Collect all asset files produced within the time window
- Also read `CAMPAIGNS/${SLUG}/DEVIATIONS.md` if it exists, filter for positioning_drift entries (per D-13)

## Step 4: Evaluate Each Asset Against GATE-01
For each collected asset:
1. Read the full asset content
2. Evaluate against GATE-01's 3 checks (from gate-evaluation.md):
   - Check 1: Differentiator alignment -- does asset restate or extend primary differentiator?
   - Check 2: Proof point sourcing -- are all claims backed by POSITIONING.md proof points?
   - Check 3: Must-not-say compliance -- does asset avoid must-not-say terms?
3. Record per-check result: PASS / WARN / FAIL
4. Calculate per-asset drift %: (WARN+FAIL count) / 3 * 100

## Step 5: Calculate Aggregates
- Total assets evaluated
- Aggregate drift %: total (WARN+FAIL) / (total assets * 3) * 100
- Count by drift category (differentiator, proof point, must-not-say)
- Cross-reference accepted deviations from DEVIATIONS.md files (per D-13)

## Step 6: Trend Comparison (per D-03)
Read `.marketing/DRIFT-LOG.md`. Find the most recent entry with event type `audit`.
If found: extract the prior aggregate drift % from the details field. Calculate delta.
Display trend arrow: up (worse), down (better), stable (same within 5%).
If no prior audit: display "First audit -- no trend data available."

## Step 7: Generate Report
Follow the report format from @positioning-check-report.md.
Display the report to stdout (per A1 -- cross-campaign, no single campaign dir).

## Step 8: Log to DRIFT-LOG.md
```bash
node "${CLAUDE_PLUGIN_ROOT}/bin/ttm-tools.cjs" drift-log append \
  --event-type audit \
  --source "/ttm-positioning-check" \
  --details "${WINDOW} audit: ${AGGREGATE_DRIFT}% drift, ${DRIFT_COUNT} findings across ${ASSET_COUNT} assets" \
  --affected ${ASSET_COUNT}
```

## Step 9: Completion Banner
```
takeToMarket > POSITIONING AUDIT COMPLETE

Window: last ${WINDOW} | Assets audited: ${ASSET_COUNT}
Aggregate drift: ${AGGREGATE_DRIFT}% [TREND_ARROW vs last audit]
Campaigns covered: ${CAMPAIGN_COUNT}

Drift logged to: .marketing/DRIFT-LOG.md
Next: Run /ttm-positioning-shift if drift > 30% warrants a positioning change
```

</process>
```

Include `<success_criteria>` and `<output>` XML blocks at the end following verify.md pattern.

Per D-01: Time-window based sampling (default 30 days, configurable via --since).
Per D-02: Auto-suggest after milestones. (Note: The actual auto-suggest insertion happens in ship.md -- add a note in this workflow that it can be invoked manually or auto-suggested.)
Per D-03: Audit output includes per-asset drift %, types of drift, aggregate drift %, and trend comparison.
Per D-13: Cross-references per-campaign accepted deviations from DEVIATIONS.md.
Per Pitfall 1: Drift defined as (WARN+FAIL checks) / total checks.
Per Pitfall 2: Exclude archived campaigns from sampling.
Per Pitfall 4: Report format extracted to positioning-check-report.md to keep workflow under 500 lines.

**Step 3: Update skills/ttm-positioning-check/SKILL.md**

Remove the line `**Status:** Not yet implemented (Phase 6)`.
Keep all other content unchanged (frontmatter, routing instruction, description bullet list).

**IMPORTANT: Total line count for positioning-check.md must stay under 500 lines. The report format, drift categories, and calculation logic are in positioning-check-report.md. The workflow only contains the process steps.**
  </action>
  <verify>
    <automated>cd /Users/rishikeshranjan/code/rishiPersonal/takeToMarket && wc -l workflows/reference-mgmt/positioning-check.md && grep -c "GATE-01" workflows/reference-mgmt/positioning-check.md && grep -c "drift-log append" workflows/reference-mgmt/positioning-check.md && grep -c "Not yet implemented" skills/ttm-positioning-check/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - workflows/reference-mgmt/positioning-check.md exists and is under 500 lines
    - workflows/reference-mgmt/positioning-check.md contains `<purpose>` and `</purpose>` tags
    - workflows/reference-mgmt/positioning-check.md contains `<required_reading>` with gate-evaluation.md and positioning-check-report.md
    - workflows/reference-mgmt/positioning-check.md contains `campaign list --since` CLI call
    - workflows/reference-mgmt/positioning-check.md contains `drift-log append --event-type audit` CLI call
    - workflows/reference-mgmt/positioning-check.md references all 3 GATE-01 checks (differentiator, proof point, must-not-say)
    - workflows/reference-mgmt/positioning-check.md contains trend comparison logic (reads prior audit from DRIFT-LOG.md)
    - workflows/reference-mgmt/positioning-check.md contains `<success_criteria>` block
    - references/positioning-check-report.md exists and contains `## Drift Categories` and `## Report Template`
    - references/positioning-check-report.md contains per-asset and aggregate drift calculation formulas
    - skills/ttm-positioning-check/SKILL.md does NOT contain "Not yet implemented"
    - skills/ttm-positioning-check/SKILL.md contains `disable-model-invocation: false`
  </acceptance_criteria>
  <done>Positioning drift audit workflow created with GATE-01 reuse, time-window sampling, aggregate/per-asset drift reporting, trend comparison, DEVIATIONS.md cross-referencing, and DRIFT-LOG.md logging. SKILL.md updated from stub to final.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Asset content -> AI evaluation | AI reads user-produced content to evaluate drift -- content may contain prompt injection attempts |
| CLI output -> workflow logic | Campaign list JSON parsed by workflow to determine sampling scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Tampering | positioning-check asset evaluation | accept | AI evaluates asset text against POSITIONING.md -- inherent to skill architecture. Standard Claude guardrails apply. No additional mitigation beyond existing GATE-01 pattern. |
| T-06-06 | Information Disclosure | audit report content | accept | Report displays positioning data and drift findings -- single-user skill, all data is local |
</threat_model>

<verification>
- positioning-check.md under 500 lines
- GATE-01 3-check evaluation logic referenced via @gate-evaluation.md
- Campaign enumeration via CLI (not direct fs operations in workflow)
- DRIFT-LOG.md append via CLI (not direct file writes in workflow)
- Report format extracted to reference file
- SKILL.md stub updated to final
</verification>

<success_criteria>
- Complete drift audit workflow with all 9 steps
- Report includes per-asset drift %, aggregate drift %, trend comparison, deviation cross-reference
- All CLI calls use the primitives created in Plan 01
- Workflow stays under 500-line limit
</success_criteria>

<output>
After completion, create `.planning/phases/06-positioning-invariant-system/06-02-SUMMARY.md`
</output>
