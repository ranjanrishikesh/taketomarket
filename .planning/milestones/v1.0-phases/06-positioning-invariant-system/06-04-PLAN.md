---
phase: 06-positioning-invariant-system
plan: 04
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-03
files_modified:
  - workflows/reference-mgmt/positioning-shift.md
  - templates/migration-plan.md
  - skills/ttm-positioning-shift/SKILL.md
autonomous: false
requirements:
  - POSN-03
  - POSN-05

must_haves:
  truths:
    - "User runs /ttm-positioning-shift and is asked for explicit reasoning for the change"
    - "User provides new positioning using all structured fields (differentiator, category, target audience, proof points, must-not-say)"
    - "If active campaigns exist, workflow generates a migration plan showing per-asset recommended action (re-verify, re-produce, accept-as-is)"
    - "User sets a deprecation schedule for shipped assets with deadlines and tracking"
    - "Workflow presents a before/after diff of all POSITIONING.md fields for human approval"
    - "Only on explicit Approve does POSITIONING.md get updated"
    - "Old positioning is archived in POSITIONING.md History table before new positioning is written"
    - "Shift event is appended to DRIFT-LOG.md via CLI"
    - "Deprecation entries are appended to DRIFT-LOG.md deprecation backlog via CLI"
  artifacts:
    - path: "workflows/reference-mgmt/positioning-shift.md"
      provides: "Controlled positioning shift workflow with approval gate"
      contains: "POSITIONING SHIFT APPROVAL"
    - path: "templates/migration-plan.md"
      provides: "Per-campaign migration recommendation template"
      contains: "Migration Plan"
    - path: "skills/ttm-positioning-shift/SKILL.md"
      provides: "Updated from stub to final"
  key_links:
    - from: "workflows/reference-mgmt/positioning-shift.md"
      to: "bin/ttm-tools.cjs campaign list --active"
      via: "CLI invocation for active campaign check"
      pattern: "campaign list --active"
    - from: "workflows/reference-mgmt/positioning-shift.md"
      to: "bin/ttm-tools.cjs drift-log append"
      via: "CLI invocation for shift logging"
      pattern: "drift-log append.*event-type shift"
    - from: "workflows/reference-mgmt/positioning-shift.md"
      to: "bin/ttm-tools.cjs drift-log deprecation"
      via: "CLI invocation for deprecation backlog"
      pattern: "drift-log deprecation"
    - from: "workflows/reference-mgmt/positioning-shift.md"
      to: ".marketing/POSITIONING.md"
      via: "Read current, write updated with History table archive"
      pattern: "POSITIONING.md"
---

<objective>
Create the /ttm-positioning-shift controlled repositioning workflow with reasoning collection, migration planning for active campaigns, deprecation scheduling, before/after diff presentation, mandatory human approval gate, and atomic POSITIONING.md update with History table archival.

Purpose: Delivers the controlled positioning change capability (POSN-03) that ensures positioning never changes silently -- every shift is reasoned, approved, and logged with a migration plan.

Output: 3 files (2 new, 1 updated SKILL.md).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-positioning-invariant-system/06-RESEARCH.md
@.planning/phases/06-positioning-invariant-system/06-PATTERNS.md
@.planning/phases/06-positioning-invariant-system/06-01-SUMMARY.md

<interfaces>
<!-- Key interfaces the executor needs -->

From templates/reference-files/positioning.md (POSITIONING.md structure):
```markdown
<!-- _SUMMARY: ... -->
**Category:** ...
**Primary Differentiator:** ...
**Target Audience:** ...
<!-- END_SUMMARY -->

## Proof Point Library
| # | Claim | Source | Last Verified |
...

## Must-Not-Say Terms
| Term | Reason |
...

### Positioning History
| Date | Change | Reasoning |
|------|--------|-----------|
| [init date] | Initial positioning | Created via /ttm-init |
```

From workflows/lifecycle/review.md (AskUserQuestion approval gate pattern):
```markdown
Using AskUserQuestion (or text-mode), ask:
- header: "..."
- question: "..."
- options:
  - label: "Approve" -- ...
  - label: "Revise" -- ...
  - label: "Cancel" -- ...

**Text-mode fallback:** Present as numbered list per standard pattern.
```

From bin/ttm-tools.cjs CLI (created in Plan 01):
```bash
# Active campaign check (per D-08/D-09)
node "${CLAUDE_PLUGIN_ROOT}/bin/ttm-tools.cjs" campaign list --active --raw
# Returns: { "campaigns": [...], "count": N }

# Drift log append (shift event)
node "${CLAUDE_PLUGIN_ROOT}/bin/ttm-tools.cjs" drift-log append \
  --event-type shift \
  --source "/ttm-positioning-shift" \
  --details "[change summary]" \
  --affected [active campaign count]

# Deprecation backlog entry
node "${CLAUDE_PLUGIN_ROOT}/bin/ttm-tools.cjs" drift-log deprecation \
  --asset "[asset name]" \
  --campaign "[campaign slug]" \
  --old-element "[what changed]" \
  --required-update "[what needs updating]" \
  --deadline "[ISO date]"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration-plan.md template and positioning-shift.md workflow</name>
  <files>templates/migration-plan.md, workflows/reference-mgmt/positioning-shift.md, skills/ttm-positioning-shift/SKILL.md</files>
  <read_first>
    - workflows/lifecycle/review.md (primary analog -- AskUserQuestion approval gate, structured interview, state update patterns)
    - workflows/lifecycle/verify.md (workflow wrapper pattern, text-mode detection, context loading, completion banner)
    - templates/reference-files/positioning.md (POSITIONING.md template -- History table format, structured fields)
    - skills/ttm-positioning-shift/SKILL.md (current stub to update)
    - templates/drift-log.md (created in Plan 01 -- deprecation backlog schema)
    - gates/gate-evaluation.md (GATE-01 checks -- used for migration plan asset evaluation)
  </read_first>
  <action>
**Step 1: Create `templates/migration-plan.md`**

```markdown
# Migration Plan: Positioning Shift

**Generated:** [ISO_TIMESTAMP]
**Shift Reasoning:** [REASONING]

## Active Campaign Impact

| Campaign | Phase | Assets | Recommended Action | User Decision |
|----------|-------|--------|-------------------|---------------|
| [slug] | [phase] | [count] | [re-verify / re-produce / accept-as-is] | [pending] |

## Per-Asset Recommendations

### Campaign: [slug]

| Asset | Conflict Type | Recommendation | Notes |
|-------|--------------|----------------|-------|
| [asset file] | [differentiator / proof point / must-not-say] | [re-verify / re-produce / accept-as-is] | [specific conflict description] |

## Deprecation Schedule (Shipped Assets)

| Asset | Campaign | Old Positioning Element | Required Update | Deadline |
|-------|----------|------------------------|-----------------|----------|
| [asset] | [campaign] | [element] | [update needed] | [date] |
```

**Step 2: Create `workflows/reference-mgmt/positioning-shift.md`**

Following the verify.md/review.md wrapper pattern, create the controlled positioning shift workflow:

```markdown
<purpose>
Controlled positioning shift workflow for /ttm-positioning-shift. Requires explicit
reasoning for the change, collects new positioning fields, generates a migration plan
for active campaigns, sets deprecation schedule for shipped assets, presents a
before/after diff for mandatory human approval, and atomically updates POSITIONING.md
with History table archival. Logs all changes to .marketing/DRIFT-LOG.md.
</purpose>

<required_reading>
@${CLAUDE_PLUGIN_ROOT}/references/context-loading.md
@${CLAUDE_PLUGIN_ROOT}/templates/reference-files/positioning.md
@${CLAUDE_PLUGIN_ROOT}/templates/migration-plan.md
</required_reading>

<process>

## Text-Mode Detection
[Same pattern as verify.md]

## Step 1: Load Context
[Load Tier 1 summaries from all 9 .marketing/ reference files]
[Load Tier 2: .marketing/POSITIONING.md (full -- needed for before/after diff)]

## Step 2: Check for Active Campaigns (per D-08, D-09)
```bash
ACTIVE_JSON=$(node "${CLAUDE_PLUGIN_ROOT}/bin/ttm-tools.cjs" campaign list --active --raw)
ACTIVE_COUNT=$(echo "$ACTIVE_JSON" | node -e "const d=require('fs').readFileSync('/dev/stdin','utf8');console.log(JSON.parse(d).count)")
```

If ACTIVE_COUNT > 0:
Display warning:
```
takeToMarket > ACTIVE CAMPAIGNS DETECTED

${ACTIVE_COUNT} campaign(s) have active assets that may conflict with
a positioning change. A migration plan will be generated.

Campaigns: [list campaign slugs and phases]
```

If ACTIVE_COUNT == 0:
Display:
```
takeToMarket > NO ACTIVE CAMPAIGNS

No active campaigns detected. Shift will apply to all future campaigns.
A migration plan is not required, but a deprecation schedule for
previously shipped assets may still be needed.
```

## Step 3: Collect Shift Information (per D-06)

### 3a: Reasoning
Using AskUserQuestion (text-mode fallback):
- header: "Positioning Shift Reasoning"
- question: "Why is this positioning change needed? Provide specific reasoning (market shift, customer feedback, competitive pressure, etc.)"
- type: free text

Record the reasoning.

### 3b: New Positioning Fields
Collect ALL structured positioning fields (matching POSITIONING.md template):
1. Primary Differentiator phrase
2. Category (market category)
3. Target Audience (who this is for)
4. Proof Points (numbered list of verifiable claims with sources)
5. Must-Not-Say Terms (terms to avoid with reasons)

For each field, show the CURRENT value and ask for the NEW value.
Use AskUserQuestion for each field (text-mode: numbered prompts).

### 3c: Migration Plan for Active Campaigns (per D-04, only if ACTIVE_COUNT > 0)
For each active campaign:
- List all assets in CAMPAIGNS/<slug>/ASSETS/
- Quick-evaluate each asset against the NEW positioning (using GATE-01 3-check logic):
  - Does the asset align with the NEW differentiator?
  - Are claims backed by NEW proof points?
  - Does the asset avoid NEW must-not-say terms?
- For each asset, recommend:
  - **re-verify**: Asset may still pass under new positioning (minor drift)
  - **re-produce**: Asset fundamentally conflicts with new positioning
  - **accept-as-is**: Asset is channel/format where positioning is less critical

Present the migration plan per campaign. Ask user to confirm or override each recommendation.

### 3d: Deprecation Schedule (per D-05)
Ask user to set deprecation timeline for shipped assets:
- "Set a deadline for updating old-positioning assets (e.g., 90 days from now)"
- Collect deadline date

For each campaign with shipped assets (from campaign list):
- List shipped assets
- Record deprecation entry: asset, campaign, old positioning element, required update, deadline

## Step 4: Present Before/After Diff and Approval Gate (per D-06, D-07)

Display the complete change for review:

```
takeToMarket > POSITIONING SHIFT APPROVAL

## Current Positioning
**Category:** [current]
**Primary Differentiator:** [current]
**Target Audience:** [current]
**Proof Points:** [current count] points
**Must-Not-Say:** [current count] terms

## Proposed Positioning
**Category:** [proposed]
**Primary Differentiator:** [proposed]
**Target Audience:** [proposed]
**Proof Points:** [proposed count] points
**Must-Not-Say:** [proposed count] terms

## What Changed
[field-by-field diff showing only changed fields]

## Reasoning
[user's stated reasoning]

## Impact
- Active campaigns affected: [count]
- Migration actions needed: [count]
- Deprecation items: [count]
- Deprecation deadline: [date]
```

Using AskUserQuestion (text-mode fallback):
- header: "Approve Positioning Shift"
- question: "Review the changes above. This will update POSITIONING.md and affect all future campaigns."
- options:
  - label: "Approve" -- Apply the new positioning
  - label: "Revise" -- Go back and modify the proposed positioning
  - label: "Cancel" -- Abandon the shift entirely

**On "Revise":** Loop back to Step 3b to re-collect fields.
**On "Cancel":** Display "Positioning shift cancelled. No changes made." and exit.
**On "Approve":** Proceed to Step 5.

## Step 5: Apply Changes (per D-07)

### 5a: Archive Old Positioning in History Table
Read current POSITIONING.md. Find the `### Positioning History` table.
Append a new row:
```
| [ISO date] | [summary of what changed] | [user's reasoning, sanitized to 100 chars] |
```
Sanitize the reasoning text: strip pipes, backticks, newlines, limit to 100 chars.

### 5b: Update POSITIONING.md Fields
Update the _SUMMARY block with new Category, Primary Differentiator, Target Audience.
Update the Proof Point Library table with new proof points.
Update the Must-Not-Say Terms table with new terms.
Preserve the `<!-- _SUMMARY -->` and `<!-- END_SUMMARY -->` delimiters.
Write the updated POSITIONING.md atomically via the Write tool.

### 5c: Log Shift to DRIFT-LOG.md
```bash
node "${CLAUDE_PLUGIN_ROOT}/bin/ttm-tools.cjs" drift-log append \
  --event-type shift \
  --source "/ttm-positioning-shift" \
  --details "Changed: [list of changed fields]. Reasoning: [first 100 chars]" \
  --affected ${ACTIVE_COUNT}
```

### 5d: Log Deprecation Entries
For each deprecation item collected in Step 3d:
```bash
node "${CLAUDE_PLUGIN_ROOT}/bin/ttm-tools.cjs" drift-log deprecation \
  --asset "[asset name]" \
  --campaign "[campaign slug]" \
  --old-element "[changed positioning element]" \
  --required-update "[what needs updating]" \
  --deadline "[ISO date]"
```

## Step 6: Completion Banner

```
takeToMarket > POSITIONING SHIFT COMPLETE

Old positioning archived in POSITIONING.md History table.
New positioning is now active for all future campaigns.

Changes logged: .marketing/DRIFT-LOG.md
Deprecation items: [count] (deadline: [date])
Active campaigns: [count] -- review migration plan above

Next steps:
[If active campaigns]: Re-run /ttm-verify <slug> for campaigns marked "re-verify"
[If deprecation items]: Update shipped assets before [deadline]
[Always]: Run /ttm-positioning-check periodically to monitor drift
```

</process>
```

Include `<success_criteria>` and `<output>` blocks following verify.md pattern.

Per D-04: Audit + recommend for in-flight campaigns. User decides per campaign.
Per D-05: Deprecation schedule tracked in DRIFT-LOG.md deprecation backlog.
Per D-06: 5-part shift requirements (reasoning, new positioning, migration, deprecation, approval).
Per D-07: Atomic update -- archive old, write new. All subsequent campaigns use new positioning.
Per D-10: Positioning-shift checks campaign state before allowing changes.
Per D-11: Even with no active campaigns, shift workflow is required (not direct edits).
Per D-12: History table gets intentional shifts only. DRIFT-LOG.md gets the full audit trail.
Per Pitfall 3: Sanitize History table entries to prevent delimiter breakage.
Per Pitfall 5: If invoked from verify escalation, the warning about active campaigns is informational.

**IMPORTANT: Total line count for positioning-shift.md must stay under 500 lines.**

**Step 3: Update skills/ttm-positioning-shift/SKILL.md**

Remove the line `**Status:** Not yet implemented (Phase 6)`.
Keep all other content unchanged (frontmatter, routing instruction, description bullet list).
  </action>
  <verify>
    <automated>cd /Users/rishikeshranjan/code/rishiPersonal/takeToMarket && wc -l workflows/reference-mgmt/positioning-shift.md && grep -c "POSITIONING SHIFT APPROVAL" workflows/reference-mgmt/positioning-shift.md && grep -c "drift-log append" workflows/reference-mgmt/positioning-shift.md && grep -c "drift-log deprecation" workflows/reference-mgmt/positioning-shift.md && grep -c "Not yet implemented" skills/ttm-positioning-shift/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - workflows/reference-mgmt/positioning-shift.md exists and is under 500 lines
    - workflows/reference-mgmt/positioning-shift.md contains `<purpose>` with "Controlled positioning shift workflow"
    - workflows/reference-mgmt/positioning-shift.md contains `campaign list --active` CLI call
    - workflows/reference-mgmt/positioning-shift.md contains `drift-log append --event-type shift` CLI call
    - workflows/reference-mgmt/positioning-shift.md contains `drift-log deprecation` CLI call
    - workflows/reference-mgmt/positioning-shift.md contains `POSITIONING SHIFT APPROVAL` banner
    - workflows/reference-mgmt/positioning-shift.md contains `AskUserQuestion` with Approve/Revise/Cancel options
    - workflows/reference-mgmt/positioning-shift.md contains `### Positioning History` table append logic
    - workflows/reference-mgmt/positioning-shift.md contains `<!-- _SUMMARY -->` and `<!-- END_SUMMARY -->` preservation instructions
    - templates/migration-plan.md exists and contains `## Active Campaign Impact` and `## Per-Asset Recommendations`
    - templates/migration-plan.md contains `## Deprecation Schedule`
    - skills/ttm-positioning-shift/SKILL.md does NOT contain "Not yet implemented"
    - skills/ttm-positioning-shift/SKILL.md contains `disable-model-invocation: true`
  </acceptance_criteria>
  <done>Positioning shift workflow created with 6-step process: context loading, active campaign check, information collection (reasoning, fields, migration, deprecation), approval gate, atomic POSITIONING.md update with History archival, and DRIFT-LOG.md logging. SKILL.md updated from stub to final.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete positioning invariant system: drift-log CLI, campaign list CLI, positioning-check audit workflow, positioning-shift controlled change workflow, read-only enforcement in all lifecycle workflows, context-loading matrix update, auto-suggest in ship workflow.</what-built>
  <how-to-verify>
    1. Verify CLI tools work:
       - Run `node bin/ttm-tools.cjs campaign list --raw` -- should return JSON with campaigns array
       - Run `node bin/ttm-tools.cjs drift-log append --event-type audit --source "test" --details "manual test" --affected 0 --raw` -- should create/append to .marketing/DRIFT-LOG.md
    2. Verify read-only enforcement:
       - Open each lifecycle workflow (brief, produce, verify, review, fix, ship) and confirm the `<constraints>` block is present between `</required_reading>` and `<process>`
    3. Verify workflows exist and are under 500 lines:
       - `wc -l workflows/reference-mgmt/positioning-check.md` (should be < 500)
       - `wc -l workflows/reference-mgmt/positioning-shift.md` (should be < 500)
    4. Verify SKILL.md stubs updated:
       - Neither skills/ttm-positioning-check/SKILL.md nor skills/ttm-positioning-shift/SKILL.md should contain "Not yet implemented"
    5. Verify auto-suggest in ship.md:
       - Open workflows/lifecycle/ship.md and confirm the positioning check auto-suggest step exists near the end
    6. Verify context-loading.md:
       - Check that references/context-loading.md has a row for /ttm-positioning-shift
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input -> POSITIONING.md writes | User provides new positioning text that gets written to POSITIONING.md |
| Migration plan -> user decisions | AI recommends actions but user decides per-campaign |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | Tampering | POSITIONING.md History table injection | mitigate | Sanitize History table row content: strip pipes, backticks, newlines, limit reasoning to 100 chars. Preserve _SUMMARY/END_SUMMARY delimiters. Same sanitization as deviation.cjs. |
| T-06-10 | Repudiation | Positioning change without audit trail | mitigate | Every shift logged to DRIFT-LOG.md via CLI append. History table in POSITIONING.md provides quick reference. Dual-log approach per D-12. |
| T-06-11 | Tampering | Prompt injection via new positioning fields | accept | User provides positioning text that AI writes to POSITIONING.md. Inherent to skill architecture. Standard Claude guardrails apply. |
</threat_model>

<verification>
- positioning-shift.md under 500 lines
- Approval gate requires explicit "Approve" before any writes
- POSITIONING.md updated atomically with History table archival
- Deprecation entries logged to DRIFT-LOG.md
- Active campaign check warns but does not block
- Migration plan shows per-asset recommendations
</verification>

<success_criteria>
- Complete shift workflow with all 6 steps
- Human approval gate blocks writes until approved
- History table archived before new positioning written
- DRIFT-LOG.md receives shift event and deprecation entries
- Migration plan generated for active campaigns
- Workflow stays under 500-line limit
</success_criteria>

<output>
After completion, create `.planning/phases/06-positioning-invariant-system/06-04-SUMMARY.md`
</output>
