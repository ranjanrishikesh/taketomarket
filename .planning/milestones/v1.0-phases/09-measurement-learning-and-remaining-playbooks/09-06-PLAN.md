---
phase: 09-measurement-learning-and-remaining-playbooks
plan: 06
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - workflows/lifecycle/verify.md
autonomous: true
requirements: [META-01, META-02, META-03, META-04]

must_haves:
  truths:
    - "Meta-gates fire during /ttm-verify alongside quality gates as Step 4c"
    - "All 4 meta-gates are Tier 2 advisory and do not block verification"
    - "Meta-gates access cross-campaign data via campaign list --raw CLI"
    - "Meta-gate results appear in the verification report after discipline gates"
  artifacts:
    - path: "workflows/lifecycle/verify.md"
      provides: "Step 4c meta-gate evaluation integrated into verify workflow"
      contains: "Step 4c"
  key_links:
    - from: "workflows/lifecycle/verify.md"
      to: "references/meta-gate-evaluation.md"
      via: "Step 4c loads evaluation instructions via @-syntax"
      pattern: "meta-gate-evaluation.md"
    - from: "workflows/lifecycle/verify.md"
      to: "bin/lib/campaign.cjs"
      via: "campaign list --raw for cross-campaign data"
      pattern: "campaign list"
---

<objective>
Integrate the 4 meta-gates (portfolio balance, calendar collision, theme consistency, learning plan) into the verify.md workflow as Step 4c. Meta-gates fire during /ttm-verify alongside base and discipline gates per D-11. All are Tier 2 advisory per D-12. They access cross-campaign data via campaign list --raw per D-13.

Purpose: Catches portfolio-level issues (over-indexing on one channel, calendar collisions, theme drift, missing learning plans) that individual asset gates cannot detect.
Output: verify.md updated with Step 4c meta-gate evaluation (~25-30 lines added, references meta-gate-evaluation.md via @-syntax).
</objective>

<execution_context>
@/Users/rishikeshranjan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rishikeshranjan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-measurement-learning-and-remaining-playbooks/09-RESEARCH.md
@.planning/phases/09-measurement-learning-and-remaining-playbooks/09-01-SUMMARY.md

<interfaces>
<!-- From workflows/lifecycle/verify.md: current structure (499 lines) -->
Current step order:
  Step 1: Load Context
  Step 2: Validate Campaign State
  Step 3: Load Assets
  Step 4: Evaluate Base Gates (10 gates)
  Step 4a: Apply Base Gate Overrides
  Step 4b: Evaluate Discipline Gates
  -- Step 4c goes HERE --
  Step 5: Build Summary Table
  Step 6: Handle Tier 1 Deviations
  Step 7: Write Verification Report
  Step 8: Display Summary
  Step 9: Update Campaign State

verify.md is currently 499 lines. The meta-gate step must be compact (~25 lines inline)
with detailed evaluation instructions loaded from references/meta-gate-evaluation.md via @-syntax.

<!-- From references/meta-gate-evaluation.md (created in Plan 01): evaluation criteria -->
Contains PASS/WARN/FAIL criteria for META-01 through META-04.
Loaded via @-syntax to keep verify.md under 500 lines.

<!-- From gates/gate-evaluation.md: evaluation output format -->
Gate result format: { gate_id, tier, result: PASS|WARN|FAIL, findings: [] }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Step 4c meta-gate evaluation to verify.md</name>
  <files>workflows/lifecycle/verify.md</files>
  <read_first>
    - workflows/lifecycle/verify.md (full file -- need to understand current structure to insert Step 4c correctly between Step 4b and Step 5)
    - references/meta-gate-evaluation.md (meta-gate evaluation instructions created in Plan 01 -- confirm it exists and has all 4 gates)
    - gates/gate-evaluation.md (gate result format for consistency)
  </read_first>
  <action>
**IMPORTANT:** verify.md is currently 499 lines. The additions must be minimal to stay under 500 lines. Use @-syntax reference to meta-gate-evaluation.md for detailed evaluation criteria. The inline step should be ~20-25 lines. To create space, look for any redundant blank lines or overly verbose comments that can be trimmed (1-2 lines) without losing meaning.

Insert the following Step 4c block between the Step 4b section ending (the "---" separator after line 261) and the Step 5 section. Add a separator line before and after.

```markdown
---

## Step 4c: Evaluate Meta-Gates

Meta-gates evaluate portfolio-level concerns across all active campaigns. All 4 meta-gates
are **Tier 2 advisory** -- they produce findings in the report but do NOT block verification.

Load detailed evaluation instructions:
@${CLAUDE_PLUGIN_ROOT}/references/meta-gate-evaluation.md

1. Fetch all campaign data:
   ```bash
   node "${CLAUDE_PLUGIN_ROOT}/bin/ttm-tools.cjs" campaign list --raw
   ```
2. Include the current campaign (${SLUG}) in the evaluation even if not yet in "active" list
3. Read `.marketing/CALENDAR.md` for quarterly theme and launch dates
4. Evaluate each meta-gate per the criteria in meta-gate-evaluation.md:
   - META-01: Portfolio Balance (funnel stage + channel diversity)
   - META-02: Calendar Collision (launch date overlap + audience collision)
   - META-03: Theme Consistency (quarterly theme alignment)
   - META-04: Learning Plan (measurement plan + testable hypothesis)
5. Record each result as: { gate_id: "META-XX", tier: 2, result: PASS|WARN|FAIL, findings: [] }
6. Append meta-gate results to a separate PORTFOLIO_RESULTS array (not mixed with per-asset gates)

Meta-gate results are displayed in a separate section of the verification report, after the
per-asset summary table.
```

Additionally, update **Step 5: Build Summary Table** to include a meta-gate section after the per-asset table:
- After the existing per-asset gate matrix, add:
```markdown
If PORTFOLIO_RESULTS is not empty, add a portfolio-level section:

### Portfolio Assessment (Tier 2 Advisory)

| Meta-Gate | Result | Finding |
|-----------|--------|---------|
| META-01: Portfolio Balance | [PASS|WARN|FAIL] | [one-line finding] |
| META-02: Calendar Collision | [PASS|WARN|FAIL] | [one-line finding] |
| META-03: Theme Consistency | [PASS|WARN|FAIL] | [one-line finding] |
| META-04: Learning Plan | [PASS|WARN|FAIL] | [one-line finding] |

> These are advisory findings. No action is required to proceed with verification.
```

Update **Step 6** (Handle Tier 1 Deviations) to explicitly note: "Meta-gates are Tier 2 advisory and are NOT included in Tier 1 deviation handling."

Update the **<checklist>** at the end to add:
```
- [ ] Meta-gates evaluated against portfolio data (campaign list --raw)
- [ ] Meta-gate results are Tier 2 advisory (not blocking)
- [ ] Portfolio Assessment section included in verification report
```

**Line budget strategy:** To stay under 500 lines after adding ~25 lines of Step 4c content, trim:
- Remove any double blank lines (reduce to single)
- Compress any comment-only lines that restate what the code already says
- Look for verbose "display" blocks that can lose 1-2 lines
Target: net addition of 0-5 lines after trimming.
  </action>
  <verify>
    <automated>grep -c "Step 4c" workflows/lifecycle/verify.md && grep -c "META-01\|META-02\|META-03\|META-04" workflows/lifecycle/verify.md && grep -c "meta-gate-evaluation.md" workflows/lifecycle/verify.md && grep -c "Tier 2" workflows/lifecycle/verify.md && wc -l workflows/lifecycle/verify.md | awk '{print ($1 <= 520) ? "OK" : "OVER_LIMIT: "$1}'</automated>
  </verify>
  <acceptance_criteria>
    - workflows/lifecycle/verify.md contains "Step 4c" section header
    - workflows/lifecycle/verify.md contains "meta-gate-evaluation.md" (@-syntax reference)
    - workflows/lifecycle/verify.md contains "campaign list --raw" (cross-campaign data source per D-13)
    - workflows/lifecycle/verify.md contains META-01, META-02, META-03, META-04 gate references
    - workflows/lifecycle/verify.md contains "Tier 2 advisory" or "Tier 2" in the meta-gate section
    - workflows/lifecycle/verify.md contains "Portfolio Assessment" in the summary table section
    - Step 4c appears between Step 4b and Step 5 in the document
    - workflows/lifecycle/verify.md is under 520 lines (target under 500, allow small buffer)
    - Meta-gates are explicitly NOT included in Tier 1 deviation handling (Step 6)
  </acceptance_criteria>
  <done>verify.md has Step 4c meta-gate evaluation that fires all 4 meta-gates as Tier 2 advisory during /ttm-verify, with cross-campaign data access and portfolio assessment section in the report.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| campaign list --raw output -> meta-gate evaluation | Cross-campaign data read from filesystem; AI evaluates in context |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-11 | Information Disclosure | Cross-campaign data | accept | All campaign data belongs to the same user; no cross-tenant risk in single-user skill |
| T-09-12 | Denial of Service | campaign list with many campaigns | accept | campaign list --raw returns frontmatter only; even 100 campaigns would be under 50KB of text |
</threat_model>

<verification>
- Step 4c exists between Step 4b and Step 5
- Meta-gates reference meta-gate-evaluation.md via @-syntax
- All 4 meta-gates listed (META-01 through META-04)
- All meta-gates classified as Tier 2 advisory
- Portfolio Assessment section added to verification report
- verify.md stays under 520 lines
</verification>

<success_criteria>
Meta-gates are integrated into the verify workflow as Step 4c per D-11. All 4 meta-gates fire as Tier 2 advisory per D-12. Cross-campaign data is accessed via campaign list --raw per D-13. Portfolio assessment appears in the verification report.
</success_criteria>

<output>
After completion, create `.planning/phases/09-measurement-learning-and-remaining-playbooks/09-06-SUMMARY.md`
</output>
