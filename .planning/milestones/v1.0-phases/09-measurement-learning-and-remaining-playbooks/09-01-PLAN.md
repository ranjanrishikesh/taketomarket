---
phase: 09-measurement-learning-and-remaining-playbooks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/lib/campaign.cjs
  - templates/measurement-report.md
  - references/measurement-template.md
  - references/meta-gate-evaluation.md
autonomous: true
requirements: [LIFE-14, LIFE-15, META-01, META-02, META-03, META-04]

must_haves:
  truths:
    - "campaign.cjs accepts measure.* and learn.* state fields without error"
    - "Measurement report template exists with outcome-first ordering"
    - "Measurement paste template exists with batch-grouped metric prompts"
    - "Meta-gate evaluation reference exists with PASS/WARN/FAIL criteria for all 4 meta-gates"
  artifacts:
    - path: "bin/lib/campaign.cjs"
      provides: "Expanded ALLOWED_FIELDS for measure/learn tracking"
      contains: "measure.run_count"
    - path: "templates/measurement-report.md"
      provides: "MEASUREMENT.md output template"
      contains: "Outcome Assessment"
    - path: "references/measurement-template.md"
      provides: "Analytics paste template for users"
      contains: "Traffic Metrics"
    - path: "references/meta-gate-evaluation.md"
      provides: "Meta-gate evaluation instructions for verify.md Step 4c"
      contains: "META-01"
  key_links:
    - from: "templates/measurement-report.md"
      to: "workflows/lifecycle/measure.md"
      via: "measure.md references template for report generation"
      pattern: "measurement-report.md"
    - from: "references/meta-gate-evaluation.md"
      to: "workflows/lifecycle/verify.md"
      via: "verify.md Step 4c loads via @-syntax"
      pattern: "meta-gate-evaluation.md"
---

<objective>
Create the infrastructure files needed by the measurement, learning, and meta-gate workflows. This includes expanding campaign.cjs ALLOWED_FIELDS for measure/learn state tracking, creating the measurement report template (outcome-first per D-05/D-06), the analytics paste template for user data collection (batch-grouped per D-02), and the meta-gate evaluation reference file with PASS/WARN/FAIL criteria for all 4 meta-gates (Tier 2 advisory per D-12).

Purpose: Downstream plans (measure.md workflow, learn.md workflow, verify.md meta-gate step) depend on these files existing. Building infrastructure first prevents blockers in Wave 2.
Output: 4 files created/modified, ready for workflow consumption.
</objective>

<execution_context>
@/Users/rishikeshranjan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rishikeshranjan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-measurement-learning-and-remaining-playbooks/09-RESEARCH.md

<interfaces>
<!-- From bin/lib/campaign.cjs: ALLOWED_FIELDS set (lines 201-221) -->
From bin/lib/campaign.cjs:
```javascript
const ALLOWED_FIELDS = new Set([
  'phase', 'name', 'last_updated',
  'phase.created', 'phase.researched', 'phase.briefed', 'phase.produced',
  'phase.verified', 'phase.reviewed', 'phase.fixed', 'phase.shipped',
  'phase.measured', 'phase.learned',
  'gate.positioning_check', 'gate.outcome_metric',
  'gate.positioning_drift', 'gate.claim_accuracy', 'gate.voice_drift',
  'gate.outcome_alignment', 'gate.funnel_integrity', 'gate.utm_hygiene',
  'gate.compliance', 'gate.competitor_collision', 'gate.icp_fit',
  'gate.format_correctness',
  'verify.run_count', 'verify.last_run', 'verify.overall_result',
  'review.run_count', 'review.last_run', 'review.overall_result',
  'fix.run_count', 'fix.last_run', 'fix.overall_result',
  'ship.status', 'ship.shipped_at', 'ship.checklist_result',
  'archive.archived_at', 'archive.learnings_extracted',
]);
```

<!-- From templates/verification-report.md: template structure pattern -->
<!-- From references/learnings-extraction.md: root-cause taxonomy categories -->
<!-- From gates/meta-gates.md: 4 meta-gate definitions (stub, needs evaluation criteria) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand campaign.cjs ALLOWED_FIELDS and create measurement report template</name>
  <files>bin/lib/campaign.cjs, templates/measurement-report.md</files>
  <read_first>
    - bin/lib/campaign.cjs (current ALLOWED_FIELDS set at line 201)
    - templates/verification-report.md (template pattern to follow for measurement-report.md)
  </read_first>
  <action>
1. In bin/lib/campaign.cjs, add the following fields to ALLOWED_FIELDS (after the 'archive.*' block, with a comment):
```javascript
  // Phase 9: Measurement tracking
  'measure.run_count', 'measure.last_run', 'measure.outcome_result',
  'measure.outcome_delta', 'measure.analytics_source',
  // Phase 9: Learn tracking
  'learn.run_count', 'learn.last_run', 'learn.lessons_extracted',
  'learn.edits_proposed', 'learn.edits_applied',
```

2. Create templates/measurement-report.md following the verification-report.md pattern. The template MUST have outcome assessment FIRST (per D-05, D-06 -- LIFE-15 compliance). Structure:
- YAML frontmatter: campaign, measured_at, analytics_source, outcome_met, outcome_delta, attribution_default
- H1: Measurement Report: ${CAMPAIGN_NAME}
- H2: Outcome Assessment (FIRST -- "Did we hit the target?" YES/NO, then table with outcome metric row BEFORE output metric rows)
- H2: Attribution Analysis (Time-Decay -- Default) with channel/weight/contribution/conversions table
- Note: "Other models (last-touch, linear) available on request."
- H2: Channel Performance (per-channel breakdown)
- H2: Signals for Learn Phase (bulleted list of overperformance/underperformance signals)
- H2: Raw Data (analytics source mode and raw pasted/collected data for audit trail)

Use ${PLACEHOLDER} syntax matching existing template conventions.
  </action>
  <verify>
    <automated>grep -c "measure.run_count" bin/lib/campaign.cjs && grep -c "learn.run_count" bin/lib/campaign.cjs && grep -c "Outcome Assessment" templates/measurement-report.md && node -e "require('./bin/lib/campaign.cjs')" 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "measure.run_count" bin/lib/campaign.cjs returns 1
    - grep -c "measure.outcome_result" bin/lib/campaign.cjs returns 1
    - grep -c "learn.lessons_extracted" bin/lib/campaign.cjs returns 1
    - grep -c "learn.edits_applied" bin/lib/campaign.cjs returns 1
    - The string "Outcome Assessment" appears before "Attribution Analysis" in templates/measurement-report.md
    - The string "Did we hit the target?" appears in templates/measurement-report.md
    - templates/measurement-report.md contains YAML frontmatter with "outcome_met" and "attribution_default: time-decay"
    - node -e "require('./bin/lib/campaign.cjs')" does not throw
  </acceptance_criteria>
  <done>campaign.cjs ALLOWED_FIELDS includes 10 new measure/learn tracking fields. Measurement report template exists with outcome-first ordering and time-decay as default attribution display.</done>
</task>

<task type="auto">
  <name>Task 2: Create measurement paste template and meta-gate evaluation reference</name>
  <files>references/measurement-template.md, references/meta-gate-evaluation.md</files>
  <read_first>
    - gates/meta-gates.md (existing 4 meta-gate definitions to expand with evaluation criteria)
    - references/review-checklist.md (reference file pattern to follow for formatting)
    - gates/gate-evaluation.md (PASS/WARN/FAIL evaluation format to replicate for meta-gates)
  </read_first>
  <action>
1. Create references/measurement-template.md -- a paste template users see when ANALYTICS_MODE=paste. Structure per D-02 (batch-grouped metrics, not one-at-a-time):

```markdown
# Measurement Data Template

Paste your analytics data in any of these formats: CSV, Markdown table, or plain text.
Group your data into these categories:

## Traffic Metrics
| Metric | Value |
|--------|-------|
| Page views | |
| Sessions | |
| Unique visitors | |
| Referral sources (top 5) | |

## Engagement Metrics
| Metric | Value |
|--------|-------|
| Average time on page | |
| Bounce rate | |
| Pages per session | |
| Scroll depth (if available) | |

## Conversion Metrics
| Metric | Value |
|--------|-------|
| Primary conversions (signups, purchases, etc.) | |
| Conversion rate | |
| Revenue (if applicable) | |
| Pipeline value (if applicable) | |

## Channel Attribution Data
| Channel | Touches | Conversions | Revenue |
|---------|---------|-------------|---------|
| | | | |

## Campaign-Specific Metrics
| Metric | Target | Actual |
|--------|--------|--------|
| [outcome metric from brief] | | |
| [output metric 1] | | |
| [output metric 2] | | |

> You can paste raw CSV, a screenshot transcript, or any structured data.
> The AI will map columns to the expected fields and ask for clarification on unmapped data.
```

2. Create references/meta-gate-evaluation.md -- full evaluation instructions for the 4 meta-gates. Each gate uses the same PASS/WARN/FAIL format as gate-evaluation.md. All gates are Tier 2 advisory per D-12. Structure:

For each meta-gate (META-01 through META-04):
- Gate ID and name
- Tier: 2 (Advisory)
- What it checks (from gates/meta-gates.md definitions)
- Data source: `campaign list --raw` CLI output + CALENDAR.md + BRIEF.md
- Evaluation criteria with specific PASS/WARN/FAIL thresholds per RESEARCH.md Code Examples section:

META-01 Portfolio Balance:
  - Funnel coverage: PASS (3+ stages), WARN (2 stages), FAIL (1 stage)
  - Channel diversity: PASS (3+ channels), WARN (2 channels), FAIL (1 channel)

META-02 Calendar Collision:
  - Launch overlap: PASS (no 2 campaigns in 3-day window), WARN (2 in same week), FAIL (3+ in same week)
  - Audience collision: PASS (different segments), WARN (same segment different channels), FAIL (same segment same channel)

META-03 Theme Consistency:
  - Theme alignment: PASS (explicit quarterly theme reference), WARN (adjacent but not explicit), FAIL (contradicts/ignores without rationale)

META-04 Learning Plan:
  - Measurement plan: PASS (outcome metric + target + window), WARN (outcome metric but missing target/window), FAIL (no outcome metric)
  - Testable hypothesis: PASS (specific hypothesis), WARN (goal but no hypothesis), FAIL (output targets only)

Include at the top: "This file is loaded by verify.md Step 4c via @-syntax. All meta-gates are Tier 2 advisory -- they produce findings in the report but do NOT block verification."

Include data collection instructions: "Run: node ${CLAUDE_PLUGIN_ROOT}/bin/ttm-tools.cjs campaign list --raw" to get all campaign states.
  </action>
  <verify>
    <automated>grep -c "Traffic Metrics" references/measurement-template.md && grep -c "META-01" references/meta-gate-evaluation.md && grep -c "META-04" references/meta-gate-evaluation.md && grep -c "Tier 2" references/meta-gate-evaluation.md && wc -l references/meta-gate-evaluation.md | awk '{print ($1 <= 500) ? "OK" : "OVER_LIMIT"}'</automated>
  </verify>
  <acceptance_criteria>
    - references/measurement-template.md contains sections: "Traffic Metrics", "Engagement Metrics", "Conversion Metrics", "Channel Attribution Data", "Campaign-Specific Metrics"
    - references/meta-gate-evaluation.md contains all 4 gate IDs: META-01, META-02, META-03, META-04
    - Every meta-gate section has PASS, WARN, and FAIL criteria
    - The string "Tier 2" or "Tier: 2" appears in references/meta-gate-evaluation.md at least 4 times
    - The string "advisory" appears in references/meta-gate-evaluation.md
    - The string "campaign list --raw" appears in references/meta-gate-evaluation.md
    - references/meta-gate-evaluation.md is under 500 lines
  </acceptance_criteria>
  <done>Measurement paste template provides batch-grouped metric prompts. Meta-gate evaluation reference has full PASS/WARN/FAIL criteria for all 4 meta-gates as Tier 2 advisory gates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User-pasted analytics data -> AI parsing | Untrusted input; AI parses, never passed to shell |
| campaign.cjs state update fields | Fields validated against ALLOWED_FIELDS set |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | campaign.cjs ALLOWED_FIELDS | mitigate | New fields added to ALLOWED_FIELDS are value-only (string/number), validated by existing parseFrontmatter serialization |
| T-09-02 | Information Disclosure | measurement-report.md | accept | Report contains campaign analytics pasted by the user; no external data exposure |
</threat_model>

<verification>
- `node -e "require('./bin/lib/campaign.cjs')"` does not throw
- All 4 new files/modifications exist and contain expected content
- templates/measurement-report.md has outcome before output metrics
- references/meta-gate-evaluation.md has all 4 META-* gates with Tier 2 classification
</verification>

<success_criteria>
campaign.cjs accepts measure.* and learn.* fields. Measurement report template enforces outcome-first ordering. Paste template groups metrics in batches. Meta-gate evaluation reference is ready for verify.md integration.
</success_criteria>

<output>
After completion, create `.planning/phases/09-measurement-learning-and-remaining-playbooks/09-01-SUMMARY.md`
</output>
